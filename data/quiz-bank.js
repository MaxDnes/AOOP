/* ============ QUIZ BANK · ~100 hand-written questions ============
   Sourced from the course content modules (solid, oop, mvvm, avalonia,
   testing, threading, linq, collections) and the two 2025 exams.
   Schema: { id, cat, type: "mc"|"code-mc"|"short", q, code?, choices?, answer, explain }
   mc / code-mc: answer = index into choices. short: answer = model answer string.
   UMD: works in the browser (window.QUIZ_BANK) and under Node (module.exports).
*/
(function (global) {
"use strict";

var BANK = [

/* ==================== OOP ==================== */

{
  id: "oop-01", cat: "OOP", type: "mc",
  q: "Which OO principle is defined as \"having one class hide information from another\"?",
  choices: [
    "Encapsulation",
    "Abstraction",
    "Polymorphism",
    "Inheritance",
  ],
  answer: 0,
  explain: "Encapsulation is information hiding: private fields, validating properties and controlled mutation methods keep a class's internals away from other code. Abstraction is the related but different idea of exposing WHAT a type does while hiding HOW, typically via interfaces. In exam code, point at private fields and private setters as encapsulation evidence.",
},
{
  id: "oop-02", cat: "OOP", type: "code-mc",
  q: "What does this print, and why?",
  code: "public class Airplane\n{\n    public virtual void Accelerate() => Console.WriteLine(\"Airplane\");\n}\npublic class Jet : Airplane\n{\n    public new void Accelerate() => Console.WriteLine(\"Jet\");\n}\n\nAirplane plane = new Jet();\nplane.Accelerate();",
  choices: [
    "\"Airplane\" — 'new' hides the base method, so the variable's static type decides",
    "\"Jet\" — the runtime type always wins",
    "Compile error — 'virtual' and 'new' cannot appear in the same hierarchy",
    "\"Jet\" then \"Airplane\" — both versions run",
  ],
  answer: 0,
  explain: "'new' hides instead of overrides, so resolution happens at compile time from the variable's type (Airplane). With 'override' the runtime type would win and it would print \"Jet\" — that is runtime polymorphism. Slide rule worth quoting: hiding members of superclasses in subclasses is usually to be avoided.",
},
{
  id: "oop-03", cat: "OOP", type: "code-mc",
  q: "What happens here?",
  code: "public class Note\n{\n    public int Pitch { get; }\n}\n\nvar note = new Note { Pitch = 30 };",
  choices: [
    "Compile error — a get-only property cannot be set in an object initializer",
    "Pitch becomes 30 — object initializers bypass accessors",
    "Runtime exception when the initializer runs",
    "Pitch stays 0 and the assignment is silently ignored",
  ],
  answer: 0,
  explain: "A { get; } property can only be assigned in the constructor or its own initializer expression. To allow object-initializer assignment while staying immutable afterwards, declare it { get; init; } — init-only setters exist exactly for this. This distinction appeared verbatim on the course slides.",
},
{
  id: "oop-04", cat: "OOP", type: "mc",
  q: "How many interfaces can a C# class implement, and how many base classes can it inherit from?",
  choices: [
    "Any number of interfaces; exactly one base class",
    "One interface; one base class",
    "Any number of both",
    "Any number of base classes; one interface",
  ],
  answer: 0,
  explain: "C# has single class inheritance but unlimited interface implementation — Duck : Animal, IFlyable, ISwimmable is legal. Interfaces are pure can-do contracts (no fields, no constructors, no implementations in this course), which is why a class can take on many of them safely. An unrelated class like Superhero can share the same contracts.",
},
{
  id: "oop-05", cat: "OOP", type: "mc",
  q: "A method takes an Airplane parameter and you pass a Jet (where Jet : Airplane). What is this conversion called?",
  choices: [
    "Upcasting — implicit and always safe",
    "Downcasting — explicit and can fail",
    "Boxing",
    "Member hiding",
  ],
  answer: 0,
  explain: "Treating a derived object as its base type is an implicit upcast: always safe, because the subclass has every member of the parent. The reverse (downcast) requires an explicit cast and can fail at runtime. airport.Start(jet) compiles; airport.Launch(plane) where Launch takes a Jet does not.",
},
{
  id: "oop-06", cat: "OOP", type: "mc",
  q: "Jet? j = plane as Jet; — what happens if plane is not actually a Jet?",
  choices: [
    "j becomes null",
    "An InvalidCastException is thrown",
    "Compile error",
    "j becomes a default-constructed Jet",
  ],
  answer: 0,
  explain: "'as' returns null on failure instead of throwing, which is why it is always paired with a null check. A hard cast (Jet)plane would throw InvalidCastException instead. The modern preferred form is pattern matching: if (plane is Jet jet) { jet.Accelerate(); } — used in the DocumentManager exam code.",
},
{
  id: "oop-07", cat: "OOP", type: "mc",
  q: "A class builds its parts inside its own constructor, keeps them in a private list, and the parts die with the whole. Which relationship is this?",
  choices: [
    "Composition",
    "Aggregation",
    "Inheritance",
    "Realization",
  ],
  answer: 0,
  explain: "Composition is the part-of relationship: the whole creates and owns its parts (House/Rooms, filled UML diamond). Aggregation is the looser has-a where parts are passed in from outside, are shareable, and survive the whole (Library/Books, hollow diamond). Code telltale: created inside + private list = composition; passed in / shareable references = aggregation.",
},
{
  id: "oop-08", cat: "OOP", type: "mc",
  q: "CompareTo returns a negative number. What does that mean?",
  choices: [
    "this sorts before the other object",
    "this sorts after the other object",
    "the two objects are equal",
    "the comparison failed",
  ],
  answer: 0,
  explain: "The IComparable contract: negative = this before other, zero = equal, positive = this after other. The course convention also returns 1 when other is null (null sorts first) and throws ArgumentException for foreign types. Implementing IComparable is what makes list.Sort() work on your own type.",
},
{
  id: "oop-09", cat: "OOP", type: "mc",
  q: "A ListBox without an ItemTemplate displays each item as what?",
  choices: [
    "The item's ToString() result",
    "The item's first public property",
    "The item's type name, always",
    "A JSON dump of the object",
  ],
  answer: 0,
  explain: "Each row shows the item's ToString(). Without an override that is the type name (e.g. \"MyApp.Models.Contact\" on every row), which is why exam model classes override it — Contact uses $\"{Name} ({Email})\", WeeklyItem uses $\"{Day}: {RecipeName}\". The alternative is a custom DataTemplate.",
},
{
  id: "oop-10", cat: "OOP", type: "mc",
  q: "What is required to give a subclass an implementation of an abstract member?",
  choices: [
    "override — abstract members must be overridden",
    "new — to replace the missing body",
    "virtual — to enable runtime dispatch",
    "Nothing; abstract members receive a default body",
  ],
  answer: 0,
  explain: "Abstract members have no body and HAVE to be overridden with 'override'. Using 'new' only hides the abstract member, leaving it unimplemented, so the code does not compile — a course slide actually contained this exact bug. 'virtual' belongs on a base member that MAY be overridden, not on the implementation.",
},
{
  id: "oop-11", cat: "OOP", type: "mc",
  q: "Which access modifier makes a member visible to subclasses but hidden from all other code?",
  choices: [
    "protected",
    "internal",
    "private",
    "sealed",
  ],
  answer: 0,
  explain: "protected = the containing class plus its subclasses. internal = anything in the same assembly, regardless of inheritance; private = the class itself only. The course's Airplane example declares 'protected int Speed' so Jet can change it but outside code cannot.",
},
{
  id: "oop-12", cat: "OOP", type: "mc",
  q: "What does ': base(speed)' after a constructor signature do?",
  choices: [
    "Calls the base-class constructor with that argument before this constructor's body runs",
    "Casts the new object to its base type",
    "Marks the constructor as overriding the base constructor",
    "Forwards speed to every base-class method call",
  ],
  answer: 0,
  explain: "Constructor chaining: public Jet(int speed, int acc) : base(speed) runs Airplane's constructor first so inherited state is initialized. Constructors are not inherited, so the subclass must explicitly forward arguments. The three inheritance syntax pieces to never fumble: 'class Jet : Airplane', ': base(speed)', and 'protected'.",
},
{
  id: "oop-13", cat: "OOP", type: "code-mc",
  q: "The GUI sets NumberOfPeople directly and the decoration cost goes stale. What is the underlying design problem?",
  code: "public class DinnerParty\n{\n    public int NumberOfPeople;          // the GUI writes this directly\n    public decimal CostOfDecorations;   // computed from NumberOfPeople\n}",
  choices: [
    "Public mutable fields let callers bypass the invariant — encapsulate with a property whose setter recalculates",
    "The fields should be static so all parties share one value",
    "decimal is the wrong type for money",
    "The class must implement IComparable to stay consistent",
  ],
  answer: 0,
  explain: "This is the course's motivating encapsulation bug from the Party Planner: a public field gives callers raw access, so dependent state is never recalculated. The fix is a private field plus a property whose setter calls CalculateCostOfDecorations, restoring the invariant automatically. Ready-made quote: \"Other developers won't always use your classes in exactly the way you expect.\"",
},
{
  id: "oop-14", cat: "OOP", type: "short",
  q: "Name the four OO principles and give a one-line definition of each.",
  answer: "Encapsulation: one class hides its internals from others (private fields, validating properties, private set). Abstraction: expose WHAT, hide HOW (interfaces and abstract classes; callers depend on ILogger, never on console details). Inheritance: is-a relationship; the derived class gains all base members (class Jet : Airplane, : base(...) chaining). Polymorphism: a base-typed variable refers to a derived object and the runtime picks the override (virtual/override, List<Party> holding mixed subtypes).",
  explain: "Problem 1 of the June 2025 exam asked exactly this: for each principle state whether it is present, where (code evidence), and what purpose it serves. If there is no class-to-class inheritance in the given code, say so explicitly and point at interface realization (Invoice : IProcessable) as the is-a mechanism instead — that was the correct move for DocumentManager.",
},
{
  id: "oop-15", cat: "OOP", type: "mc",
  q: "When does the course say to prefer composition over inheritance?",
  choices: [
    "When subclasses only share data — inherit when they share behavior that subclasses override",
    "Never — inheritance is always cleaner",
    "Whenever polymorphism is needed",
    "When the classes live in different assemblies",
  ],
  answer: 0,
  explain: "The final OOP slide solves the Party domain twice: an abstract Party base class versus an IParty interface plus a composed PartyDetails object. The guidance: inheritance pays off when shared behavior is overridden by subclasses; if they only share data, composition or an interface avoids the rigid hierarchy. Be ready to sketch both shapes.",
},

/* ==================== SOLID ==================== */

{
  id: "sol-01", cat: "SOLID", type: "mc",
  q: "\"Each software module should have one and only one reason to change\" — which principle is this (slide wording)?",
  choices: [
    "SRP — Single Responsibility",
    "OCP — Open / Closed",
    "ISP — Interface Segregation",
    "DIP — Dependency Inversion",
  ],
  answer: 0,
  explain: "That is the verbatim SRP definition from the slides — reproduce it exactly in written answers. The fast heuristic: count the jobs per class; a god class mixing UI, logic and storage has many reasons to change. FamilyMealPlanner applies SRP by splitting MealPlanner / ShoppingListGenerator / ConsoleNotifier / InMemoryRecipeRepository.",
},
{
  id: "sol-02", cat: "SOLID", type: "mc",
  q: "How do you check the Open/Closed Principle in unfamiliar exam code?",
  choices: [
    "Ask: can I add a new variant by ADDING a class, or must I EDIT an existing switch/if chain?",
    "Ask: are all fields private?",
    "Ask: does every class implement at least one interface?",
    "Ask: is there exactly one class per file?",
  ],
  answer: 0,
  explain: "OCP applied looks like new IDietaryRule implementations slotting in without touching the checker; violated looks like a type-or-kind switch that grows with every new case. Definition to quote: \"A module should be open for extension but closed for modification.\" The exams also love \"set up but undermined\" — an extension point that exists but is bypassed.",
},
{
  id: "sol-03", cat: "SOLID", type: "code-mc",
  q: "_recipeFinder is an injected IRecipeFinder. Which principle does this downcast violate most directly?",
  code: "public List<Recipe> GenerateMealPlan(FamilyProfile family, int meals)\n{\n    var inMemoryRepo = _recipeFinder as InMemoryRecipeRepository;  // downcast\n    if (inMemoryRepo == null)\n    {\n        _notifier.Notify(\"Error: finder does not support random selection.\");\n        return new List<Recipe>();\n    }\n    // ... uses inMemoryRepo.GetRandomRecipe(...) — not on the interface\n}",
  choices: [
    "LSP — other IRecipeFinder implementations are no longer substitutable",
    "SRP — the method does two jobs at once",
    "ISP — the interface has too many members",
    "OCP — a switch statement is hiding here",
  ],
  answer: 0,
  explain: "This is the planted violation from the August 2025 re-exam. A DatabaseRecipeFinder is a perfectly legal IRecipeFinder, yet the planner refuses to work with it — substitutes are not substitutable, which is the Liskov Substitution Principle. It also undermines DIP because the high-level policy depends on the concrete class again; the fix is moving GetRandomRecipe onto the interface and deleting the cast.",
},
{
  id: "sol-04", cat: "SOLID", type: "code-mc",
  q: "What is the SOLID problem in this constructor?",
  code: "public class Service\n{\n    private FileLogger _log;\n\n    public Service()\n    {\n        _log = new FileLogger();   // concrete project class\n    }\n}",
  choices: [
    "DIP violation — the class constructs its own concrete dependency instead of receiving an abstraction",
    "LSP violation — FileLogger cannot substitute for Service",
    "ISP violation — the logger interface is too fat",
    "No problem — constructors are exactly where dependencies belong",
  ],
  answer: 0,
  explain: "\"Depend upon Abstractions. Do not depend upon concretions.\" Service should declare ILogger as a constructor parameter and let the composition root (Program.cs / App.axaml.cs) choose the concrete type. That makes implementations swappable and lets unit tests inject a fake logger. Note the nuance: new-ing BCL types like List<T> is fine — new-ing your own service classes inside business logic is the smell.",
},
{
  id: "sol-05", cat: "SOLID", type: "code-mc",
  q: "Every new shape forces an edit to this method. Which principle is violated, and what is the standard fix?",
  code: "public double AreaOf(Shape s)\n{\n    if (s is Circle c) return 3.14159 * c.R * c.R;\n    else if (s is Square q) return q.Side * q.Side;\n    else if (s is Triangle t) return 0.5 * t.Base * t.Height;\n    return 0;\n}",
  choices: [
    "OCP — replace the type-check chain with an abstract GetArea() that each shape overrides",
    "SRP — split the method across three new classes",
    "DIP — inject the shapes through the constructor",
    "LSP — Square must not derive from Shape",
  ],
  answer: 0,
  explain: "An is-type chain (or a switch on a kind enum) means the module must be MODIFIED for every new variant — the textbook OCP violation. Polymorphism is the fix: declare GetArea() abstract on Shape so adding Triangle is purely additive. The same medicine in interface form is the Strategy pattern (IDietaryRule in the exam domain).",
},
{
  id: "sol-06", cat: "SOLID", type: "code-mc",
  q: "What is the diagnosis here?",
  code: "public interface IDocument\n{\n    bool Process();\n    string GenerateSummary();\n    List<string> Validate();\n}\n\npublic class Report : IDocument\n{\n    public bool Process() => true;\n    public string GenerateSummary() => \"...\";\n    public List<string> Validate()\n        => throw new NotImplementedException();   // forced junk\n}",
  choices: [
    "ISP violation — the fat interface forces Report to implement a member it cannot support",
    "SRP violation — Report has three responsibilities",
    "DIP violation — Report depends on a concretion",
    "Encapsulation violation — Validate should be private",
  ],
  answer: 0,
  explain: "\"Many client specific interfaces are better than one general purpose interface.\" A throwing or empty forced implementation is the classic ISP symptom — and the throw is simultaneously an LSP hazard for any caller holding an IDocument. The June exam's DocumentManager shows the fix: small role interfaces (IProcessable, ISummarizable, IValidatable) that classes opt into, so Report simply skips IValidatable.",
},
{
  id: "sol-07", cat: "SOLID", type: "code-mc",
  q: "Which principle does this class violate?",
  code: "public class ReportManager\n{\n    public string BuildReport(List<Sale> sales) { /* formatting */ return \"\"; }\n    public void SaveToFile(string report) { File.WriteAllText(\"r.txt\", report); }\n    public void EmailToBoss(string report) { /* smtp logic */ }\n}",
  choices: [
    "SRP — formatting, persistence and email are three reasons to change",
    "OCP — you cannot add a new report type",
    "LSP — ReportManager is not substitutable",
    "ISP — the class implements too many interfaces",
  ],
  answer: 0,
  explain: "One class, three jobs: change the layout, the storage format, or the mail server and you edit the same class — three reasons to change. The fix is splitting into ReportBuilder / ReportSaver / ReportMailer, each with exactly one responsibility. SRP at architecture scale is also why MVVM separates View, ViewModel and Model.",
},
{
  id: "sol-08", cat: "SOLID", type: "mc",
  q: "What is the relationship between Dependency Inversion (DIP) and Dependency Injection (DI)?",
  choices: [
    "DIP is the principle (depend on abstractions); DI is the technique that delivers it (push dependencies in via constructors)",
    "They are two names for the same thing",
    "DI is the principle; DIP is a framework feature",
    "DIP applies to classes, DI only to interfaces",
  ],
  answer: 0,
  explain: "DIP states the goal: high-level modules depend on abstractions, never on concretions. Constructor injection is how this course achieves it — classes declare interfaces as constructor parameters and the composition root builds and wires the concrete objects. Benefits to cite in answers: swap implementations without touching consumers, and unit tests can inject fakes.",
},
{
  id: "sol-09", cat: "SOLID", type: "mc",
  q: "Per the course, where is the ONLY place that should know concrete implementation types?",
  choices: [
    "The composition root — Program.cs in console apps, App.axaml.cs in Avalonia apps",
    "The ViewModel constructor",
    "A static ServiceLocator class",
    "Each class's own constructor",
  ],
  answer: 0,
  explain: "The composition root is the single wiring point: it news up the repository, notifier and rules, then injects them; everything downstream sees only interfaces. At the exam App.axaml.cs is read-only — you READ it to learn what your ViewModel receives, and you never construct services inside the ViewModel yourself.",
},
{
  id: "sol-10", cat: "SOLID", type: "mc",
  q: "Which design pattern is the standard enabler of OCP — a family of interchangeable algorithms behind one interface?",
  choices: [
    "Strategy",
    "Singleton",
    "Facade",
    "Observer",
  ],
  answer: 0,
  explain: "Strategy encapsulates each algorithm (VegetarianRule, NutAllergyRule) behind a common interface (IDietaryRule) so the context — rules.All(r => r.IsSatisfiedBy(...)) — never changes when a new rule is added. Other course sightings: ILogger/ConsoleLogger and the IView console views. A delegate like Func<int,int> passed into a method is a lightweight strategy.",
},
{
  id: "sol-11", cat: "SOLID", type: "mc",
  q: "How do you recognize a Singleton in exam code?",
  choices: [
    "private constructor + static instance accessor + usually sealed",
    "A class containing only static methods",
    "A class registered in App.axaml.cs",
    "Any class whose name ends in Manager",
  ],
  answer: 0,
  explain: "The course implementation is ShapeFacade: sealed, private ShapeFacade(), and a private static readonly Lazy<ShapeFacade> exposed via a static GetInstance property. Lazy<T> makes creation thread-safe and deferred to first use. Honest critique if asked: global state hurts testability — prefer DI for dependencies.",
},
{
  id: "sol-12", cat: "SOLID", type: "mc",
  q: "MVVM is built from which three patterns, per the course?",
  choices: [
    "Observer (PropertyChanged) + Command (RelayCommand) + Dependency Injection (composition root)",
    "Singleton + Facade + Strategy",
    "Factory + Adapter + Observer",
    "Strategy + Decorator + Command",
  ],
  answer: 0,
  explain: "The binding system is an Observer subscribed to INotifyPropertyChanged; every Button binds to a Command object with Execute/CanExecute; and App.axaml.cs is the composition root performing dependency injection. Saying \"MVVM = Observer + Command + DI\" with those three code locations is a compact, high-scoring theory answer.",
},
{
  id: "sol-13", cat: "SOLID", type: "code-mc",
  q: "Rules are injected but never used, and the repository hardcodes the filtering. What is the sharpest exam observation?",
  code: "public class MealPlanner\n{\n    private readonly List<IDietaryRule> _rules;\n\n    public MealPlanner(IRecipeFinder finder, INotifier notifier,\n                       List<IDietaryRule> rules)\n    {\n        _rules = rules;   // injected ... and never referenced again\n    }\n    // InMemoryRecipeRepository.FindRecipes hardcodes the filtering inline\n}",
  choices: [
    "OCP is set up via IDietaryRule but then undermined — the extension point is dead code",
    "The field should be public so Program.cs can use it",
    "LSP is violated because List<IDietaryRule> is a concrete type",
    "Nothing — unused dependencies are harmless",
  ],
  answer: 0,
  explain: "This is verbatim re-exam material: the architecture gestures at OCP (rules as injectable strategies), but the repository hardcodes vegetarian/nut filtering inline, so adding a rule class changes nothing. Writing \"the principle is present here BUT undermined there\" with the unused _rules field and a line number reads as top-grade analysis.",
},
{
  id: "sol-14", cat: "SOLID", type: "mc",
  q: "The classic Square : Rectangle problem (SetWidth/SetHeight break the caller's expectations) illustrates which principle?",
  choices: [
    "LSP — behavior promised by the base must hold for every subclass",
    "SRP — Square has two responsibilities",
    "OCP — Rectangle is not open for extension",
    "DIP — Square depends on a concretion",
  ],
  answer: 0,
  explain: "Code written against Rectangle assumes width and height vary independently; a Square that forces them equal breaks that promise, so the subtype is not substitutable. Slide wording: \"Subclasses should be substitutable for their base classes.\" The course's meme version: if it quacks like a duck but needs batteries, you have the wrong abstraction.",
},
{
  id: "sol-15", cat: "SOLID", type: "mc",
  q: "Which trio of members makes up the ICommand interface (the Command pattern)?",
  choices: [
    "Execute, CanExecute, CanExecuteChanged",
    "Run, CanRun, OnRunChanged",
    "Invoke, Validate, Notify",
    "Do, Undo, Redo",
  ],
  answer: 0,
  explain: "The Button (invoker) never knows WHAT happens — it can only ask \"may I?\" (CanExecute), say \"do it\" (Execute), and re-check itself when CanExecuteChanged fires. That decoupling of invoker from receiver is the pattern's whole point. In this course you rarely hand-write it: [RelayCommand] generates an IRelayCommand property for you.",
},
{
  id: "sol-16", cat: "SOLID", type: "short",
  q: "The re-exam planner downcasts its injected IRecipeFinder to InMemoryRecipeRepository to call GetRandomRecipe. Write the fix you would propose.",
  answer: "Move the needed capability onto the abstraction: add Recipe? GetRandomRecipe(IEnumerable<Recipe> from) to the IRecipeFinder interface (or a new focused interface), implement it in InMemoryRecipeRepository, and have MealPlanner call _recipeFinder.GetRandomRecipe(...) directly. Delete the as-cast and the \"does not support random selection\" error branch.",
  explain: "The violation is that only one concrete implementation works, so substitutes are not substitutable (LSP) and the high-level class secretly depends on a concretion again (DIP). A complete exam answer states both the mechanical change (interface member + direct call) and the principles it restores. This was THE planted violation of August 2025.",
},
{
  id: "sol-18", cat: "SOLID", type: "mc",
  q: "Which statement best describes how the Observer design pattern works?",
  choices: [
    "The Subject keeps a list of Observers and notifies them when its state changes",
    "Each Observer keeps polling the Subject in a loop to check whether anything changed",
    "The Subject and Observers share a database that they both read from",
    "Observers modify the Subject's state directly whenever they need to",
  ],
  answer: 0,
  explain: "A Subject holds a collection of registered Observers; when its state changes it PUSHES a notification to each one (observer.Update(...)) so they react — no polling, no shared store. Subscribers register and unregister via AddObserver/RemoveObserver. In C# this is exactly what events and INotifyPropertyChanged do: the binding system is the observer, your ViewModel the subject. The pattern decouples the subject from whoever is listening.",
},
{
  id: "sol-19", cat: "SOLID", type: "mc",
  q: "How does the Adapter design pattern typically let a client use a class with an incompatible interface?",
  choices: [
    "The adapter wraps the service and exposes an interface compatible with the client",
    "The adapter wraps the client so that the service can drive it instead",
    "The adapter acts as a standalone bridge that references neither class",
    "The adapter merges the client and the service into a single combined object",
  ],
  answer: 0,
  explain: "The adapter implements the interface the CLIENT already expects, holds a reference to the incompatible service (the adaptee), and translates each call into the adaptee's real API. The client talks to the adapter as if it were the expected type; the adapter forwards and reshapes the work. It must reference the adaptee — one that references neither class cannot adapt anything. Contrast Facade, which simplifies a subsystem rather than fixing incompatibility.",
},

/* ==================== Avalonia & MVVM ==================== */

{
  id: "av-01", cat: "Avalonia & MVVM", type: "mc",
  q: "In the Avalonia MVVM template, where is the application's REAL DataContext set?",
  choices: [
    "App.axaml.cs — OnFrameworkInitializationCompleted assigns desktop.MainWindow's DataContext",
    "MainWindow.axaml via Design.DataContext",
    "MainWindow.axaml.cs inside InitializeComponent",
    "Program.cs inside BuildAvaloniaApp",
  ],
  answer: 0,
  explain: "App.axaml.cs is the composition root: it builds the services, constructs the ViewModel (with its injected dependencies) and assigns it as the window's DataContext. Design.DataContext in the XAML feeds only the IDE previewer. At the exam you READ App.axaml.cs to learn what your ViewModel receives, but you never edit it.",
},
{
  id: "av-02", cat: "Avalonia & MVVM", type: "mc",
  q: "The exam UI problem says \"strictly follow the MVVM pattern\". What happens to code you add in MainWindow.axaml.cs?",
  choices: [
    "It does not count toward your solution — the UI problem is graded on ViewModel + AXAML only",
    "It counts, but with a small point deduction",
    "It is accepted as long as the app compiles",
    "Only async code is banned there",
  ],
  answer: 0,
  explain: "The exam phrasing is explicit: \"Any code you add or modify in other files will not count toward your solution... do not modify the CodeBehind (MainWindow.axaml.cs).\" Everything must be done with observable properties, commands and bindings. Translations: Click handler becomes [RelayCommand]; myTextBlock.Text = ... becomes [ObservableProperty] + {Binding}; listBox.Items.Add becomes ObservableCollection mutation.",
},
{
  id: "av-03", cat: "Avalonia & MVVM", type: "mc",
  q: "Which TWO things must be true of a class for [ObservableProperty] to work?",
  choices: [
    "It is partial AND inherits ObservableObject (directly or via ViewModelBase)",
    "It is sealed AND implements INotifyPropertyChanged manually",
    "Its fields are public AND PascalCase",
    "It is static AND lives in the ViewModels namespace",
  ],
  answer: 0,
  explain: "The source generator needs 'partial' so it can add the generated property half of the class, and ObservableObject supplies the PropertyChanged plumbing it hooks into. The annotated field must be _camelCase or camelCase; the generator emits the PascalCase property. A course slide once omitted ': ObservableObject' — without it the generator has nothing to raise notifications through.",
},
{
  id: "av-04", cat: "Avalonia & MVVM", type: "code-mc",
  q: "Why doesn't the bound TextBlock ever update?",
  code: "[ObservableProperty]\nprivate int _count;\n\n[RelayCommand]\nprivate void Increment()\n{\n    _count++;     // UI never changes!\n}",
  choices: [
    "The code mutates the backing field — use the generated property (Count++) so the setter raises PropertyChanged",
    "int properties cannot be bound; only strings can",
    "The command must be async for bindings to update",
    "The TextBlock binding needs Mode=TwoWay",
  ],
  answer: 0,
  explain: "[ObservableProperty] generates a Count property whose setter raises the change notification. Writing to the _count field bypasses that setter, so no PropertyChanged fires and the binding never repaints. Rule from the slides: in code always USE the generated PascalCase property, never the field.",
},
{
  id: "av-05", cat: "Avalonia & MVVM", type: "mc",
  q: "[RelayCommand] on 'private async Task LoadDataAsync()' generates a command named what?",
  choices: [
    "LoadDataCommand — the Async suffix is dropped",
    "LoadDataAsyncCommand",
    "LoadData",
    "ExecuteLoadDataAsync",
  ],
  answer: 0,
  explain: "Name generation: void Foo() produces FooCommand, and an Async suffix is removed first, so Task LoadDataAsync() produces LoadDataCommand. Async methods generate an AsyncRelayCommand, which automatically disables the bound button while running. Knowing the generated name matters because the AXAML binds {Binding LoadDataCommand}.",
},
{
  id: "av-06", cat: "Avalonia & MVVM", type: "mc",
  q: "A command's CanExecute guard reads Count, but the button never re-enables when Count changes. Why?",
  choices: [
    "CanExecute is not re-evaluated automatically — call NotifyCanExecuteChanged() or use [NotifyCanExecuteChangedFor]",
    "The guard must be a property, not a method",
    "Buttons evaluate CanExecute only once at startup by design",
    "The Command binding must be TwoWay",
  ],
  answer: 0,
  explain: "The UI re-checks the guard only when the command's CanExecuteChanged event fires, and that does not happen by itself. Either implement partial void OnCountChanged(...) and call DecrementCommand.NotifyCanExecuteChanged(), or put [NotifyCanExecuteChangedFor(nameof(DecrementCommand))] on the _count field. The June 2025 Counter problem tested exactly this wiring.",
},
{
  id: "av-07", cat: "Avalonia & MVVM", type: "mc",
  q: "How many direct children can a Window (or a Border) contain in AXAML?",
  choices: [
    "Exactly one — wrap siblings in a panel",
    "Any number",
    "Two — content plus a menu",
    "One per RowDefinition",
  ],
  answer: 0,
  explain: "Window, Border and UserControl take exactly ONE child; two siblings directly inside a Window is a compile error. The single child is usually a layout panel (StackPanel, Grid, DockPanel) which then holds everything else. This is one of the first things to check when AXAML refuses to build.",
},
{
  id: "av-08", cat: "Avalonia & MVVM", type: "mc",
  q: "What does x:DataType on the Window root element give you?",
  choices: [
    "Compiled bindings — paths are checked against that type at build time, so a typo becomes a compile error",
    "It sets the runtime DataContext",
    "It makes all bindings TwoWay",
    "It registers the ViewModel for dependency injection",
  ],
  answer: 0,
  explain: "x:DataType enables compiled bindings (the exam projects set AvaloniaUseCompiledBindingsByDefault), so a misspelled property name fails the build instead of failing silently at runtime — a gift under exam pressure. Gotcha: inside a DataTemplate or style that binds against a different type, you need another x:DataType there (see the ContentPresenter style for Canvas items).",
},
{
  id: "av-09", cat: "Avalonia & MVVM", type: "mc",
  q: "In a Grid with ColumnDefinitions=\"Auto,*,2*\", how is the width distributed?",
  choices: [
    "Column 0 sizes to its content; the leftover space splits 1:2 between columns 1 and 2",
    "Three equal columns",
    "Column 0 gets nothing until the others are full",
    "2* is invalid syntax",
  ],
  answer: 0,
  explain: "Auto = size to content, * = one share of the remaining space, 2* = two shares, and a plain number (e.g. 120) is fixed pixels. The same rules apply to RowDefinitions. The shorthand attribute form RowDefinitions=\"Auto,*,Auto\" is exam-standard; the long form with <RowDefinition> elements is equivalent.",
},
{
  id: "av-10", cat: "Avalonia & MVVM", type: "mc",
  q: "Two Grid children render on top of each other. The most likely cause?",
  choices: [
    "Neither sets Grid.Row / Grid.Column, so both default to row 0, column 0",
    "The Grid is missing ShowGridLines=\"True\"",
    "Grids require an ItemTemplate",
    "Their Z-index values are equal",
  ],
  answer: 0,
  explain: "Children default to row 0, column 0 when the attached properties are not set, so they silently overlap — the classic Grid bug. Always set Grid.Row and Grid.Column explicitly on every child. Attached properties are set ON the child but read BY the parent panel (same mechanism as DockPanel.Dock and Canvas.Left).",
},
{
  id: "av-11", cat: "Avalonia & MVVM", type: "code-mc",
  q: "The rectangles all render at 0,0 even though X and Y are bound. Why?",
  code: "<ItemsControl.ItemTemplate>\n  <DataTemplate>\n    <Rectangle Width=\"{Binding Width}\" Height=\"{Binding Height}\"\n               Canvas.Left=\"{Binding X}\" Canvas.Top=\"{Binding Y}\"/>\n  </DataTemplate>\n</ItemsControl.ItemTemplate>",
  choices: [
    "Template content is wrapped in a ContentPresenter, which is the actual Canvas child — set Canvas.Left/Top on it via an ItemsControl style",
    "Canvas.Left only accepts literal numbers, never bindings",
    "The DataTemplate needs Mode=TwoWay",
    "Rectangles cannot be placed on a Canvas",
  ],
  answer: 0,
  explain: "Attached Canvas properties only matter on the panel's DIRECT children, and each templated item gets wrapped in a ContentPresenter — so setting them on the Rectangle does nothing. The fix is a style: <Style Selector=\"ContentPresenter\" x:DataType=\"models:RectangleData\"> with setters binding Canvas.Left/Top to X/Y. This was the most exotic part of the June exam, and the starter file already contained the structure.",
},
{
  id: "av-12", cat: "Avalonia & MVVM", type: "mc",
  q: "You replace a bound ObservableCollection with a NEW instance through a plain get-only property. What happens?",
  choices: [
    "The UI silently keeps watching the old collection — mutate the existing instance, or make the property notify",
    "The UI updates automatically because ObservableCollection always notifies",
    "A binding exception is thrown at runtime",
    "The ListBox clears and stays empty forever",
  ],
  answer: 0,
  explain: "ObservableCollection notifies about ITEM changes (add/remove/clear), not about the property being swapped to a new instance — without a property-level notification the View still observes the old object. Two valid strategies: keep a get-only ObservableCollection and mutate it, or do what the official re-exam solution did: [ObservableProperty] private List<WeeklyItem> weekPlan; and assign a NEW list, which notifies via the property setter.",
},
{
  id: "av-13", cat: "Avalonia & MVVM", type: "mc",
  q: "Which binding mode is the default for user-editable properties like TextBox.Text, CheckBox.IsChecked and Slider.Value?",
  choices: [
    "TwoWay",
    "OneWay",
    "OneTime",
    "OneWayToSource",
  ],
  answer: 0,
  explain: "User-editable properties default to TwoWay so typing or sliding flows back into the ViewModel; display-only properties like TextBlock.Text default to OneWay (ViewModel to View). OneTime takes a single snapshot at load. For a binding to update the UI at runtime at all, the source must notify — [ObservableProperty] for values, ObservableCollection for lists.",
},
{
  id: "av-14", cat: "Avalonia & MVVM", type: "mc",
  q: "What is the MVVM-correct way to react when the user selects a ListBox row?",
  choices: [
    "Bind SelectedItem to an [ObservableProperty] and implement the OnXxxChanged partial method",
    "Handle the SelectionChanged event in code-behind",
    "Poll the ListBox from a timer",
    "Bind ItemsSource with Mode=TwoWay",
  ],
  answer: 0,
  explain: "SelectedItem=\"{Binding SelectedRecipe}\" pushes the selection into the ViewModel, and the generator's partial void OnSelectedRecipeChanged(value) hook runs after every change — the re-exam's master-detail task (show the selected recipe's ingredients) lived exactly in that method. A SelectionChanged handler in code-behind would score zero on the UI problem.",
},
{
  id: "av-15", cat: "Avalonia & MVVM", type: "mc",
  q: "In MVVM, which layer is allowed to reference UI controls (TextBlock, Button, ...)?",
  choices: [
    "Only the View — ViewModels never touch controls, and Models are plain .NET classes",
    "The ViewModel, since it drives the UI",
    "Both the View and the ViewModel",
    "Any layer, as long as bindings exist",
  ],
  answer: 0,
  explain: "Three near-verbatim slide statements worth quoting: Views must not contain logic; ViewModels must not touch UI controls; Models are POCOs with no framework dependency. The ViewModel exposes observable state and commands, and the View renders them via bindings. That separation is exactly what makes the ViewModel unit-testable without launching a window.",
},
{
  id: "av-16", cat: "Avalonia & MVVM", type: "short",
  q: "Explain MVVM in the context of an exam app like RectangleUI — the standard 5-point theory answer.",
  answer: "MVVM separates UI from logic. The Model (RectangleData) holds the data for one rectangle. The ViewModel (MainWindowViewModel) holds presentation state (the rectangle collection, slider values) and logic (add/shuffle commands) exposed as observable properties; it never touches controls. The View (MainWindow.axaml) declares the layout and connects to the ViewModel purely through data bindings and commands; PropertyChanged notifications flow back so the UI updates automatically. Benefits: the logic is unit-testable without UI, and View and logic can change independently.",
  explain: "The scoring recipe: name the three layers, say WHICH class plays each role in the given project, and state the two glue mechanisms — bindings/commands from View to ViewModel, and change notifications from ViewModel back to View. Closing with the testability benefit rounds out the marks.",
},

/* ==================== Testing ==================== */

{
  id: "tst-01", cat: "Testing", type: "mc",
  q: "What do the three As of the AAA test pattern stand for?",
  choices: [
    "Arrange, Act, Assert",
    "Assume, Apply, Approve",
    "Arrange, Assert, Act",
    "Act, Arrange, Audit",
  ],
  answer: 0,
  explain: "Arrange creates the object under test and its inputs, Act calls the thing being tested, Assert checks the outcome — in that order, with one logical assertion theme per test. Course naming conventions: test class ClassUnderTest_MethodShould, test method Method_Scenario_ExpectedResult (e.g. IsPrime_InputIs1_ReturnFalse).",
},
{
  id: "tst-02", cat: "Testing", type: "mc",
  q: "In xUnit's Assert.Equal, which argument comes first?",
  choices: [
    "The expected value — Assert.Equal(expected, actual)",
    "The actual value",
    "Order doesn't matter",
    "The precision/tolerance",
  ],
  answer: 0,
  explain: "xUnit's convention is expected first, actual second; swapping them makes every failure message lie (\"expected 5, got 3\" when it was the reverse). For floating point there is an overload taking a precision argument as the third parameter. Small detail, but it shows up in graded test code.",
},
{
  id: "tst-03", cat: "Testing", type: "mc",
  q: "What does [Theory] with [InlineData(...)] give you over [Fact]?",
  choices: [
    "One test body executed once per data row, with the row's values as parameters",
    "Parallel test execution",
    "Tests that only run in Release builds",
    "Automatic mocking of the parameters",
  ],
  answer: 0,
  explain: "[Fact] is a single fixed case; [Theory] reruns the same body for every [InlineData] row, with the values bound to the method's parameters — e.g. IsPrime tested with (1,false), (2,true), (7,true), (9,false) in four lines. It keeps coverage broad without copy-pasted test methods.",
},
{
  id: "tst-04", cat: "Testing", type: "mc",
  q: "How do you test that dividing by zero throws the right exception?",
  choices: [
    "Assert.Throws<DivideByZeroException>(() => calc.Divide(1, 0))",
    "try/catch around the call with a bool flag and Assert.True",
    "Assert.Equal(DivideByZeroException, calc.Divide(1, 0))",
    "[Fact(ExpectedException = typeof(DivideByZeroException))]",
  ],
  answer: 0,
  explain: "Assert.Throws takes the exact exception type and a lambda that should throw; it fails if nothing (or a different type) is thrown, and returns the exception for further asserts. The lambda matters: calling the method directly as an argument would throw before the assertion machinery ever runs.",
},
{
  id: "tst-05", cat: "Testing", type: "mc",
  q: "Which attribute must headless Avalonia UI tests use?",
  choices: [
    "[AvaloniaFact] — never plain [Fact]",
    "[Fact(Headless = true)]",
    "[UITest]",
    "[STAFact]",
  ],
  answer: 0,
  explain: "[AvaloniaFact] runs the test on Avalonia's dispatcher so controls actually work. The other mandatory setup: an assembly-level [assembly: AvaloniaTestApplication(typeof(TestAppBuilder))] whose builder calls UseHeadless(new AvaloniaHeadlessPlatformOptions()), and window.Show() before interacting. Forgetting any of the three is a classic failure.",
},
{
  id: "tst-06", cat: "Testing", type: "mc",
  q: "First thing to call in a headless test after constructing MainWindow with its DataContext?",
  choices: [
    "window.Show() — required even though nothing actually renders",
    "window.Activate()",
    "Dispatcher.Run()",
    "Nothing — construction is enough",
  ],
  answer: 0,
  explain: "Show() makes the window 'live' so layout, focus and input simulation work; without it FindControl, KeyTextInput and enabled-state checks misbehave even though no pixels are drawn. The course's own example calls window.Show() immediately after setting the DataContext, with a comment that it is required.",
},
{
  id: "tst-07", cat: "Testing", type: "code-mc",
  q: "What is this test checking?",
  code: "var vm = new MainWindowViewModel();\nAssert.False(vm.DecrementCommand.CanExecute(null));\n\nvm.IncrementCommand.Execute(null);   // Count: 0 -> 1\n\nAssert.True(vm.DecrementCommand.CanExecute(null));",
  choices: [
    "The CanExecute guard: disabled at Count 0, enabled once Count > 0 — without ever running Decrement",
    "That Decrement actually decrements the counter",
    "That the button is visible in the window",
    "That PropertyChanged fires exactly once",
  ],
  answer: 0,
  explain: "CanExecute(null) evaluates the guard predicate without executing the command; Execute(null) actually runs it. Because property writes go through the generated setters, OnCountChanged hooks and NotifyCanExecuteChangedFor fire exactly like in the real app. The June exam's five required Counter tests are variations of this pattern.",
},
{
  id: "tst-08", cat: "Testing", type: "mc",
  q: "window.FindControl<Button>(\"IncrementButton\") returns null in your headless test. Most likely cause?",
  choices: [
    "The control has no Name=\"IncrementButton\" in the AXAML",
    "Headless mode strips all buttons",
    "You must use x:Key instead of Name",
    "FindControl only works after a click",
  ],
  answer: 0,
  explain: "FindControl locates controls by their Name attribute, and named controls also become fields on the window class. The June exam required asserting the controls exist (Assert.NotNull), which meant first ADDING Name attributes to the starter AXAML. Once found, control.IsEffectivelyEnabled reflects the bound command's CanExecute state.",
},
{
  id: "tst-09", cat: "Testing", type: "mc",
  q: "How do you simulate the user typing \"10\" into a TextBox in a headless test?",
  choices: [
    "control.Focus(); then window.KeyTextInput(\"10\")",
    "control.Text = \"10\"",
    "window.SendKeys(\"10\")",
    "Dispatcher.UIThread.Type(\"10\")",
  ],
  answer: 0,
  explain: "KeyTextInput types into whatever has focus, going through the real input pipeline so TwoWay bindings push the text into the ViewModel — setting .Text directly would bypass user-input behaviors. For key presses use window.KeyPressQwerty(PhysicalKey.Enter, RawInputModifiers.None); Enter on a focused button counts as a click.",
},
{
  id: "tst-10", cat: "Testing", type: "mc",
  q: "Why does MVVM make ViewModels easy to unit test?",
  choices: [
    "A ViewModel is a plain class — instantiate it, set properties, execute commands, assert; no Avalonia required",
    "The MVVM Toolkit generates the tests",
    "Bindings can be asserted directly",
    "Because ViewModels are static",
  ],
  answer: 0,
  explain: "The ViewModel holds all the logic but never references UI types, so a test can new it up, poke its generated properties and commands, and assert on the results without launching a window. That is precisely why the exam splits 'normal' VM tests from headless UI tests — and why logic in code-behind is untestable and banned.",
},
{
  id: "tst-11", cat: "Testing", type: "mc",
  q: "The exam-day commands to create and wire a test project, in order?",
  choices: [
    "dotnet new xunit -o Counter.Tests; dotnet add Counter.Tests reference Counter/Counter.csproj; dotnet test",
    "dotnet new test; dotnet link Counter; dotnet run",
    "dotnet add xunit; dotnet new reference; dotnet test",
    "dotnet new xunit; dotnet test --connect Counter",
  ],
  answer: 0,
  explain: "Create the test project, add a project reference so it can see the app's classes, run. For headless UI tests there is a fourth command: dotnet add Counter.Tests package Avalonia.Headless.XUnit --version 11.2.1. Run all of these once at home so every package sits in the local NuGet cache and restores offline at the exam.",
},
{
  id: "tst-12", cat: "Testing", type: "short",
  q: "Name the three classic headless-test failures the course warns about.",
  answer: "1) Forgot window.Show() before interacting with the window. 2) Used [Fact] instead of [AvaloniaFact]. 3) Controls have no Name attribute in the AXAML, so FindControl and the generated fields return null. Fix all three before debugging anything else.",
  explain: "These three account for nearly every mysteriously failing headless test. The full setup checklist: assembly-level [assembly: AvaloniaTestApplication(typeof(TestAppBuilder))] with UseHeadless, [AvaloniaFact] on each test, manual DataContext wiring, window.Show(), and Name attributes on every control you need to find or assert on.",
},

/* ==================== Threading & Async ==================== */

{
  id: "thr-01", cat: "Threading & Async", type: "mc",
  q: "The course's definition of a race condition requires which TWO conditions?",
  choices: [
    "Multiple threads access shared data AND at least one of them changes it",
    "Two threads run at different priorities",
    "There are more threads than CPU cores",
    "A lock is held for more than 100 ms",
  ],
  answer: 0,
  explain: "Concurrent reads alone are safe; the trouble starts when shared access is combined with at least one writer — then thread scheduling decides the outcome, making it unpredictable and hard to debug. The fix is making the critical section exclusive with lock, or using thread-safe collections.",
},
{
  id: "thr-02", cat: "Threading & Async", type: "code-mc",
  q: "What does this print?",
  code: "int counter = 0;\nvar tasks = new List<Task>();\nfor (int i = 0; i < 10; i++)\n    tasks.Add(Task.Run(() =>\n    {\n        for (int j = 0; j < 1000; j++)\n            counter++;\n    }));\nawait Task.WhenAll(tasks);\nConsole.WriteLine(counter);",
  choices: [
    "Usually LESS than 10000 — counter++ is read/modify/write, so updates get lost",
    "Exactly 10000 — Task.Run is thread-safe",
    "Always 1000 — only one task really runs",
    "A random number above 10000",
  ],
  answer: 0,
  explain: "counter++ is three steps: read the value, increment it, write it back. Two threads can read the same value, both increment, and both write the same result — one increment is lost. Wrapping the increment in lock (_lockObj) { counter++; } serializes the critical section and restores exactly 10000. This is the canonical lost-update demo from the lecture.",
},
{
  id: "thr-03", cat: "Threading & Async", type: "mc",
  q: "What is the lock best practice from the slides?",
  choices: [
    "Lock on a dedicated private readonly object and keep the critical section as small as possible",
    "Lock on this",
    "Lock on typeof(MyClass)",
    "Always lock on the shared data object itself",
  ],
  answer: 0,
  explain: "A dedicated private readonly object _lock = new(); guarantees no foreign code can lock the same reference and interfere — locking 'this' or any publicly reachable object risks exactly that. Keeping the locked section minimal limits contention so threads spend less time waiting.",
},
{
  id: "thr-04", cat: "Threading & Async", type: "mc",
  q: "Task 1 locks A then B; Task 2 locks B then A; they hang forever. What is the fix?",
  choices: [
    "Acquire locks in the same global order on every code path",
    "Add more locks to spread the contention",
    "Insert Thread.Sleep before the second lock",
    "Make both lock objects static",
  ],
  answer: 0,
  explain: "This is a deadlock: each task holds one lock and waits forever for the other. The cause as taught is acquiring multiple locks in INCONSISTENT order; the fix is a global lock ordering (every path takes A before B). Sleeps only change the timing — they cannot remove the cyclic wait.",
},
{
  id: "thr-05", cat: "Threading & Async", type: "code-mc",
  q: "What is the output order?",
  code: "Console.WriteLine(\"A\");\nTask task = Task.Run(() =>\n{\n    Thread.Sleep(500);\n    Console.WriteLine(\"B\");\n});\nConsole.WriteLine(\"C\");\nawait task;\nConsole.WriteLine(\"D\");",
  choices: [
    "A, C, B, D",
    "A, B, C, D",
    "A, C, D, B",
    "A, B, D, C",
  ],
  answer: 0,
  explain: "The task starts running when CREATED by Task.Run, not when awaited — but it sleeps 500 ms first, so \"C\" prints before \"B\". The await then suspends without blocking until the task completes (\"B\"), after which the continuation prints \"D\". Two rules answer most ordering puzzles: tasks start at the call, and await yields control instead of blocking.",
},
{
  id: "thr-06", cat: "Threading & Async", type: "mc",
  q: "Why is calling task.Result (or .Wait()) inside a UI command dangerous?",
  choices: [
    "It blocks the UI thread — frozen window and potential deadlock; await instead",
    "It discards the task's return value",
    "It silently swallows exceptions",
    "It is removed in .NET 9",
  ],
  answer: 0,
  explain: ".Result and .Wait() block the calling thread until the task finishes; on the UI thread nothing can repaint meanwhile, and if the task itself needs the UI thread you deadlock. Also know the exception difference: Wait()/Result wrap failures in AggregateException, while await rethrows the original exception type.",
},
{
  id: "thr-07", cat: "Threading & Async", type: "mc",
  q: "IO-bound vs CPU-bound work — which tool for which?",
  choices: [
    "IO-bound: async/await on naturally-async APIs (no extra thread); CPU-bound: Task.Run to move work off the current thread",
    "Both: Task.Run",
    "IO-bound: Task.Run; CPU-bound: async/await",
    "Both: raw Thread objects",
  ],
  answer: 0,
  explain: "During IO (network, disk, Task.Delay) the CPU is idle — awaiting frees the thread, no extra thread needed. CPU-bound work (heavy math, image processing, intensive LINQ) actually occupies a core, so push it onto the pool with Task.Run to keep the caller responsive. This summary table from the lecture is a likely direct exam question.",
},
{
  id: "thr-08", cat: "Threading & Async", type: "mc",
  q: "When is 'async void' acceptable?",
  choices: [
    "Only for event handlers",
    "Whenever you don't need a result",
    "Never",
    "Only inside unit tests",
  ],
  answer: 0,
  explain: "async void cannot be awaited and its exceptions cannot be caught by the caller, so the rule is async Task everywhere — except event handlers, whose signature is fixed. The Avalonia file-dialog Click handlers are the course's sanctioned example. Commands should be async Task methods; [RelayCommand] then generates an AsyncRelayCommand.",
},
{
  id: "thr-09", cat: "Threading & Async", type: "mc",
  q: "How do you correctly stop a long-running task from the outside, per the slides?",
  choices: [
    "CancellationTokenSource + passing its Token into the task — never a bool flag",
    "Thread.Abort()",
    "task.Dispose()",
    "Setting a static volatile bool",
  ],
  answer: 0,
  explain: "The CancellationTokenSource is the remote control (the owner calls .Cancel()); the CancellationToken is the read-only view the task receives. React either by polling token.IsCancellationRequested, or by passing the token to awaitable calls (Task.Delay, WaitAsync, WaitForNextTickAsync) which then throw OperationCanceledException — catch it as the NORMAL shutdown signal, not an error.",
},
{
  id: "thr-10", cat: "Threading & Async", type: "mc",
  q: "What is the difference between Task.WhenAll and Task.WhenAny?",
  choices: [
    "WhenAll completes when ALL tasks finish; WhenAny completes when the FIRST one finishes and returns the winner",
    "WhenAll runs tasks sequentially; WhenAny in parallel",
    "WhenAll is async; WhenAny blocks the thread",
    "They differ only in how exceptions are wrapped",
  ],
  answer: 0,
  explain: "WhenAll awaits a whole batch (the results array preserves argument order); WhenAny races them and returns the first finisher. The lecture's timeout pattern builds on WhenAny: race the operation against Task.Delay(3s), and if the delay wins, report a timeout and cancel the loser via the shared CancellationTokenSource.",
},
{
  id: "thr-11", cat: "Threading & Async", type: "code-mc",
  q: "Both fetches take ~2 seconds. How long does this take, and what is the concurrent fix?",
  code: "var a = await FetchAsync(\"page1\");\nvar b = await FetchAsync(\"page2\");",
  choices: [
    "~4s — create both tasks first, then await Task.WhenAll(t1, t2) to finish in ~2s",
    "~2s — awaits run in parallel automatically",
    "~4s — and it cannot be improved in C#",
    "~2s — the compiler reorders independent awaits",
  ],
  answer: 0,
  explain: "Awaiting immediately serializes the calls: the second fetch doesn't even start until the first completes. Tasks begin when CALLED, so create both first — var t1 = FetchAsync(...); var t2 = FetchAsync(...); — then await Task.WhenAll(t1, t2). Both run concurrently and the total is roughly the slower one. This is the most common async trap in the course.",
},
{
  id: "thr-12", cat: "Threading & Async", type: "mc",
  q: "An exam task says \"all UI updates must happen on the UI thread\". Which two standard tools satisfy it?",
  choices: [
    "DispatcherTimer (ticks on the UI thread), or Dispatcher.UIThread.Post(...) around bound-property writes from background threads",
    "lock around every property write",
    "Task.Run around every property write",
    "Thread.Sleep between updates",
  ],
  answer: 0,
  explain: "DispatcherTimer's Tick handler already runs on the UI thread, so mutating bound properties there is safe by construction. If work runs on a background thread (Task.Run loop), marshal each UI-facing write with Dispatcher.UIThread.Post(() => Count++) or await InvokeAsync. Exam tip: state the argument in a one-line comment over the mutation to make the points undeniable.",
},
{
  id: "thr-13", cat: "Threading & Async", type: "mc",
  q: "What does SemaphoreSlim(3, 3) give you that lock cannot?",
  choices: [
    "Up to 3 threads inside the section concurrently — lock allows exactly one",
    "Faster lock acquisition",
    "Automatic deadlock detection",
    "Reentrancy on the same thread",
  ],
  answer: 0,
  explain: "A semaphore is the bouncer at a nightclub with N slots: WaitAsync takes a slot (awaitable, so no thread is blocked while queuing) and Release returns it. The lecture pattern: await _throttler.WaitAsync(token); try { work } finally { _throttler.Release(); } — the finally guarantees the slot returns even on exceptions or cancellation.",
},
{
  id: "thr-14", cat: "Threading & Async", type: "short",
  q: "The re-exam async counter: Start increments Count every 100 ms, Stop pauses (resumable), Reset zeroes. Name the three accepted implementation strategies.",
  answer: "1) DispatcherTimer with Interval = TimeSpan.FromMilliseconds(100) — Tick runs on the UI thread; Start()/Stop() call timer.Start()/timer.Stop(). 2) Task.Run loop with await Task.Delay(100, token) plus a CancellationTokenSource for Stop, marshalling Count++ through Dispatcher.UIThread.Post. 3) PeriodicTimer in an async loop: while (await timer.WaitForNextTickAsync(token)) Count++. All three keep the UI responsive; state the thread-safety argument in a comment.",
  explain: "The starter ViewModel already imported System.Threading, System.Threading.Tasks and Avalonia.Threading — a broad hint that any of the three standard solutions is welcome. Keep methods named Start/Stop/Reset so the generated StartCommand/StopCommand/ResetCommand match the existing bindings, and add the one-line thread-safety comment for the easy 2 points.",
},

/* ==================== LINQ & JSON ==================== */

{
  id: "lnq-01", cat: "LINQ & JSON", type: "mc",
  q: "What is the difference between Where and Select?",
  choices: [
    "Where filters whole items by a predicate; Select transforms each item into something else",
    "Where transforms; Select filters",
    "Where works on lists, Select on arrays",
    "Select removes duplicates",
  ],
  answer: 0,
  explain: "Where keeps the items that pass the boolean test (selection/filtering); Select projects each item into a new shape (projection), e.g. p => p.Name or p => new { p.Name, p.Price }. Chained operators read left to right like a pipeline: products.Where(p => p.Price > 500).Select(p => p.Name).ToList().",
},
{
  id: "lnq-02", cat: "LINQ & JSON", type: "mc",
  q: "Which of these operators forces a deferred LINQ query to actually execute?",
  choices: [
    "ToList()",
    "Where()",
    "OrderBy()",
    "Select()",
  ],
  answer: 0,
  explain: "Where, Select, OrderBy, GroupBy, Join and Take only BUILD the query (deferred); execution happens at ToList()/ToArray(), element operators (First/Single), aggregations (Count/Sum/Average/Min/Max) or a foreach. Bonus gotcha: a deferred query re-executes every time you iterate it — call ToList() once if you need the results twice.",
},
{
  id: "lnq-03", cat: "LINQ & JSON", type: "code-mc",
  q: "Where does \"Marker\" appear in the output?",
  code: "var query = numbers.Where(n =>\n{\n    Console.WriteLine(\"Filtering: \" + n);\n    return n % 2 == 0;\n});\nConsole.WriteLine(\"Marker\");\nvar result = query.ToList();",
  choices: [
    "Before any \"Filtering\" lines — the lambda only runs when ToList() executes the query",
    "After all \"Filtering\" lines",
    "Interleaved with the \"Filtering\" lines",
    "It never prints",
  ],
  answer: 0,
  explain: "Lazy evaluation: defining the query stores the lambda but does not run it, so \"Marker\" prints first and the \"Filtering\" lines appear only when ToList() iterates the source. Moving .ToList() directly onto the Where call flips the order. Predicting this output order is a stated likely exam question.",
},
{
  id: "lnq-04", cat: "LINQ & JSON", type: "mc",
  q: "First vs Single — what is the behavioral difference?",
  choices: [
    "First returns the first match and ignores the rest; Single throws unless there is EXACTLY one match",
    "They are aliases for the same operation",
    "Single returns null on no match; First throws",
    "First is deferred; Single is immediate",
  ],
  answer: 0,
  explain: "Single asserts uniqueness: zero or two-plus matches both throw InvalidOperationException. FirstOrDefault returns null/default instead of throwing when nothing matches. The exams used Single for \"the single matching product\"; use First when duplicates are legal and you just want one. Slide fact: these element operators have NO query-syntax equivalent.",
},
{
  id: "lnq-05", cat: "LINQ & JSON", type: "mc",
  q: "The single most exam-relevant LINQ shape — group, then aggregate per group — looks like?",
  choices: [
    "list.GroupBy(x => x.Key).Select(g => new { Type = g.Key, Avg = g.Average(...) })",
    "list.Aggregate((a, b) => a + b)",
    "list.Sum(x => x.GroupBy)",
    "list.Join(list, x => x, y => y, (x, y) => x).Distinct()",
  ],
  answer: 0,
  explain: "GroupBy yields groups, each with a .Key and itself enumerable, so a following Select projects key + aggregates (Sum/Average/Count). The June exam wanted exactly this: ships.GroupBy(s => s.Type).Select(g => new { Type = g.Key, Avg = g.Average(s => s.TravelHistory?.Count ?? 0) }) — average trips per ship type. Memorize the shape.",
},
{
  id: "lnq-06", cat: "LINQ & JSON", type: "mc",
  q: "What is the argument order of Join?",
  choices: [
    "outer.Join(inner, outerKeySelector, innerKeySelector, resultSelector)",
    "outer.Join(inner, resultSelector, outerKeySelector, innerKeySelector)",
    "Join(outer, inner, on, equals)",
    "inner.Join(outer, innerKeySelector, outerKeySelector, resultSelector)",
  ],
  answer: 0,
  explain: "Memorize: sales.Join(products, sale => sale.ProductId, product => product.Id, (sale, product) => new { product.Name, sale.QuantitySold, TotalRevenue = product.Price * sale.QuantitySold }). The result selector receives one element from each side and can compute new properties — the slide's TotalRevenue example.",
},
{
  id: "lnq-07", cat: "LINQ & JSON", type: "mc",
  q: "OrderBy vs list.Sort() — what is the difference?",
  choices: [
    "OrderBy returns a NEW sorted sequence and leaves the source untouched; Sort() mutates the list in place (using IComparable)",
    "Both sort in place",
    "Sort() returns a new list",
    "OrderBy only works on strings",
  ],
  answer: 0,
  explain: "OrderBy (and OrderByDescending/ThenBy) are LINQ operators producing a new sequence — the original list keeps its order. List<T>.Sort() rearranges the list itself and relies on the element type implementing IComparable (the Party class implements CompareTo by Date for exactly this). Know both, and that the exams always want \"most at the top\" = OrderByDescending.",
},
{
  id: "lnq-08", cat: "LINQ & JSON", type: "code-mc",
  q: "Four ships have NO TravelHistory property in the JSON. What happens here, and what is the fix?",
  code: "var byTrips = ships\n    .OrderByDescending(s => s.TravelHistory.Count)\n    .ToList();",
  choices: [
    "NullReferenceException — TravelHistory is null for those ships; use s.TravelHistory?.Count ?? 0",
    "Those ships sort last automatically",
    "JsonSerializer refuses to load the file",
    "Count returns 0 for null lists",
  ],
  answer: 0,
  explain: "Missing JSON values deserialize to null when the property is declared nullable (List<Trip>? TravelHistory), so dotting into it throws at query time. The null-conditional + null-coalescing combo (?.Count ?? 0) treats missing history as zero trips. The June exam's \"handle missing values\" points (10 of 35) hinged on exactly this.",
},
{
  id: "lnq-09", cat: "LINQ & JSON", type: "mc",
  q: "Which chain turns a list of recipes into a deduplicated, alphabetical shopping list of all ingredients?",
  choices: [
    "recipes.SelectMany(r => r.Ingredients).Distinct().OrderBy(i => i).ToList()",
    "recipes.Select(r => r.Ingredients).Distinct().ToList()",
    "recipes.GroupBy(r => r.Ingredients).Select(g => g.Key).ToList()",
    "recipes.OrderBy(r => r.Ingredients).Distinct().ToList()",
  ],
  answer: 0,
  explain: "SelectMany flattens lists-of-lists into one sequence of ingredients; plain Select would give you a list of lists. Distinct removes duplicates and OrderBy sorts alphabetically. This is literally the course's ShoppingListGenerator logic — worth citing as SRP evidence too, since that one job lives in its own class.",
},
{
  id: "lnq-10", cat: "LINQ & JSON", type: "mc",
  q: "Which call requires an explicit type argument: JsonSerializer.Serialize or Deserialize?",
  choices: [
    "Deserialize<List<Contact>>(json) — Serialize infers the type from its argument",
    "Serialize<T> — Deserialize infers from the JSON text",
    "Both require it",
    "Neither requires it",
  ],
  answer: 0,
  explain: "Serialize can see the runtime type of the object you hand it; Deserialize only has a string, so you must say what shape to build: JsonSerializer.Deserialize<List<Contact>>(File.ReadAllText(\"contacts.json\")). Pair it with a null-forgiving ! or ?? fallback, and a File.Exists check before reading (the course's load-or-empty repository pattern).",
},
{
  id: "lnq-11", cat: "LINQ & JSON", type: "mc",
  q: "The exam asks for a human-readable JSON output file. Which option does that?",
  choices: [
    "new JsonSerializerOptions { WriteIndented = true }",
    "PropertyNamingPolicy = JsonNamingPolicy.CamelCase",
    "PropertyNameCaseInsensitive = true",
    "Encoding.UTF8 on the writer",
  ],
  answer: 0,
  explain: "WriteIndented pretty-prints with newlines and indentation — the re-exam explicitly wanted readable output files. The other two options solve different problems: CamelCase changes how property names are WRITTEN, and PropertyNameCaseInsensitive tolerates case mismatches when READING someone else's JSON.",
},
{
  id: "lnq-12", cat: "LINQ & JSON", type: "mc",
  q: "How do you model JSON properties that are sometimes missing (e.g. ArrivalDate for ships still traveling)?",
  choices: [
    "Declare them nullable (DateTime?, List<Trip>?) — deserialization leaves them null instead of throwing",
    "Mark them with an [Optional] attribute",
    "Use string for every property",
    "Pre-process the JSON to insert default values",
  ],
  answer: 0,
  explain: "Nullable properties absorb missing values gracefully: a ship without TravelHistory gets null, a trip without ArrivalDate gets null (meaning \"still traveling\"). Your queries then handle null with ?. and ?? — e.g. (s.TravelHistory ?? new()).Any(t => t.ArrivalDate == null). This was worth 10 points on the June exam.",
},
{
  id: "lnq-13", cat: "LINQ & JSON", type: "mc",
  q: "Saving four query results into ONE structured JSON file (re-exam 4.2) — the course-sanctioned trick?",
  choices: [
    "Serialize an anonymous object: new { vegetarianRecipes = veg, noDietaryRestrictions = none, ... } — property names become the JSON keys",
    "Write four files and concatenate them by hand",
    "Serialize a List<object> containing the four lists",
    "Call Serialize four times and join with +",
  ],
  answer: 0,
  explain: "Anonymous-type property names become the JSON keys exactly as written, so one Serialize call produces the whole structured file: File.WriteAllText(\"Problem_4_Query_Results.json\", JsonSerializer.Serialize(results, new JsonSerializerOptions { WriteIndented = true })). Match the required key names character for character — the submission format was strict.",
},
{
  id: "lnq-14", cat: "LINQ & JSON", type: "code-mc",
  q: "This works, but what flaw does the model solution avoid?",
  code: "var aboveAverage = recipes\n    .Where(r => r.Ingredients.Count >\n                recipes.Average(x => x.Ingredients.Count))\n    .ToList();",
  choices: [
    "The average is recomputed for EVERY recipe — compute double avg = ... once, before the Where",
    "Average doesn't work on integer counts",
    "Where cannot reference the outer list",
    "It throws unless the list is sorted first",
  ],
  answer: 0,
  explain: "The predicate runs once per element, and each run re-enumerates the entire list to compute the same average — quadratic work for nothing. Hoist it: double avg = recipes.Average(r => r.Ingredients.Count); then .Where(r => r.Ingredients.Count > avg). Same result, clearer intent, and it mirrors the official re-exam solution's \"compute avg FIRST\" pattern.",
},
{
  id: "lnq-15", cat: "LINQ & JSON", type: "short",
  q: "Write the query: all recipes with NO dietary restrictions (empty DietaryTags list).",
  answer: "var noRestrictions = recipes.Where(r => r.DietaryTags.Count == 0).ToList(); — equivalently .Where(r => !r.DietaryTags.Any()). If the list could be missing from the JSON entirely, guard with (r.DietaryTags?.Count ?? 0) == 0.",
  explain: "This was re-exam query 2, worth 5 points. Note it asks for an EMPTY tag list, not a missing one — read the actual data file before assuming nullability. The other three re-exam queries: Contains(\"Vegetarian\"), OrderByDescending(r => r.Ingredients.Count), and the hoisted-average Where.",
},

/* ==================== Collections & Generics ==================== */

{
  id: "col-01", cat: "Collections & Generics", type: "mc",
  q: "You need to count word occurrences with fast lookup by word. Which collection?",
  choices: [
    "Dictionary<string, int> — hashed keys give O(1) lookup",
    "List<KeyValuePair<string, int>>",
    "HashSet<string>",
    "Queue<string>",
  ],
  answer: 0,
  explain: "Dictionary maps unique hashed keys to values with O(1) lookup — the slides list counting occurrences as a primary use case. The idiom: if (counts.ContainsKey(word)) counts[word]++; else counts[word] = 1;. A HashSet stores only the words without counts, and a List makes every lookup a O(n) scan.",
},
{
  id: "col-02", cat: "Collections & Generics", type: "code-mc",
  q: "What happens here?",
  code: "var ages = new Dictionary<string, int> { { \"Alice\", 25 } };\nConsole.WriteLine(ages[\"Bob\"]);",
  choices: [
    "KeyNotFoundException — use TryGetValue (or ContainsKey) for safe lookup",
    "Prints 0",
    "Prints an empty line",
    "Bob is silently added with value 0",
  ],
  answer: 0,
  explain: "The indexer throws on a missing key when READING. The slide-endorsed safe pattern: if (!ages.TryGetValue(\"Bob\", out int val)) Console.WriteLine(\"No val\");. Note the asymmetry: ASSIGNING through the indexer (ages[\"Bob\"] = 1) is fine — it adds or overwrites without complaint.",
},
{
  id: "col-03", cat: "Collections & Generics", type: "mc",
  q: "Queue<T> vs Stack<T> — which methods, which order?",
  choices: [
    "Queue: Enqueue/Dequeue, FIFO (supermarket line); Stack: Push/Pop, LIFO (stack of plates)",
    "Queue: Push/Pop, FIFO; Stack: Enqueue/Dequeue, LIFO",
    "Both use Add/Remove; only iteration order differs",
    "Queue is LIFO; Stack is FIFO",
  ],
  answer: 0,
  explain: "Queue is first-in-first-out: Enqueue adds at the back, Dequeue removes from the front. Stack is last-in-first-out: Push and Pop work the top. Both have Peek to look at the next element without removing it. Slide use cases: queues for task scheduling and print jobs, stacks for undo/redo and navigation history.",
},
{
  id: "col-04", cat: "Collections & Generics", type: "code-mc",
  q: "What is the output?",
  code: "var set = new HashSet<int> { 1, 2, 3 };\nbool added = set.Add(2);\nConsole.WriteLine(set.Count + \" \" + added);",
  choices: [
    "\"3 False\" — duplicates are silently ignored and Add returns false",
    "\"4 True\" — sets allow duplicates",
    "An ArgumentException is thrown",
    "\"3 True\"",
  ],
  answer: 0,
  explain: "A HashSet stores unique elements; adding a duplicate is a no-op that returns false. Contains is a fast hash lookup, there is no indexer, and set operations (UnionWith, IntersectWith, ExceptWith) come built in. The slide demo: new HashSet<char>(\"the quick brown fox\") drops duplicate letters, keeping first-seen order.",
},
{
  id: "col-05", cat: "Collections & Generics", type: "mc",
  q: "What does ObservableCollection<T> add over List<T>?",
  choices: [
    "Change notifications (INotifyCollectionChanged) so bound UI updates on Add/Remove",
    "Thread safety",
    "Faster lookups",
    "Automatic sorting",
  ],
  answer: 0,
  explain: "It is essentially a List that raises CollectionChanged events, which the binding system subscribes to — that's why every UI-bound list in the course is an ObservableCollection. It is NOT thread-safe, and it does not notify when an item's own properties change; for that, the item class itself needs [ObservableProperty] members (the RectangleData pattern).",
},
{
  id: "col-06", cat: "Collections & Generics", type: "mc",
  q: "The collection-interface capability ladder, smallest to richest?",
  choices: [
    "IEnumerable<T> (foreach only) → ICollection<T> (Add/Remove/Count) → IList<T> (indexer access)",
    "ICollection<T> → IEnumerable<T> → IList<T>",
    "IList<T> → ICollection<T> → IEnumerable<T>",
    "IEnumerable<T> → IList<T> → ICollection<T>",
  ],
  answer: 0,
  explain: "IEnumerable<T> only hands out an enumerator — \"enumeration only\", anything foreach-able and LINQ-able. ICollection<T> adds counting and mutation (\"countable\"), IList<T> adds positional access (\"rich functionality\"). Exam signatures use IEnumerable<Recipe> deliberately: accept the loosest interface you need so callers can pass any collection; return what you actually have.",
},
{
  id: "col-07", cat: "Collections & Generics", type: "mc",
  q: "What does foreach actually call under the hood?",
  choices: [
    "GetEnumerator(), then MoveNext() and Current in a loop",
    "The indexer from 0 to Count-1",
    "Select() followed by ToList()",
    "GetNext() until it returns null",
  ],
  answer: 0,
  explain: "foreach is sugar for getting an IEnumerator<T> and looping while MoveNext() returns true, reading Current each pass — the slide shows the manual expansion verbatim. That is why ANY IEnumerable works in foreach, and why modifying a collection mid-enumeration throws InvalidOperationException (the enumerator detects the change).",
},
{
  id: "col-08", cat: "Collections & Generics", type: "mc",
  q: "What is the generics advantage triple from the slides?",
  choices: [
    "Type safety (compile-time checks), performance (no boxing/conversions), reusability (one implementation, many types)",
    "Speed, security, simplicity",
    "Inheritance, polymorphism, abstraction",
    "Less memory, faster JIT, smaller binaries",
  ],
  answer: 0,
  explain: "Repeat the triple verbatim if asked. Type safety: List<int> rejects strings at compile time. Performance: values are stored without boxing into object. Reusability: one Pair<T, U> or Collector<T> works for every element type. Generic constraints like where T : class (seen in the June exam's GetAllDocumentsOfType<T>) refine what T may be.",
},
{
  id: "col-09", cat: "Collections & Generics", type: "mc",
  q: "What does 'yield return' inside a method give you?",
  choices: [
    "A lazy IEnumerable that produces elements one at a time, on demand",
    "Early method exit with a value",
    "A Task that yields the thread",
    "A sorted sequence",
  ],
  answer: 0,
  explain: "Iterator methods build an IEnumerable<T> without materializing a collection: each MoveNext() runs the body to the next yield return. Evens(10) produces 0 2 4 6 8 10 one value at a time. The same laziness underpins deferred LINQ queries — nothing happens until someone iterates.",
},
{
  id: "col-10", cat: "Collections & Generics", type: "mc",
  q: "One thread enumerates a List<int> while another calls Add() on it. What happens?",
  choices: [
    "InvalidOperationException — standard collections are not thread-safe; lock around access or use concurrent collections",
    "The new items appear mid-loop",
    "Silent data corruption, never an exception",
    "Nothing — List is thread-safe for reads",
  ],
  answer: 0,
  explain: "Enumeration detects concurrent modification and throws. Two safe strategies from the slides: snapshot inside a lock (copy with ToList, iterate the copy outside) or iterate inside the lock (simpler, lock held longer). ConcurrentBag/Queue/Stack/Dictionary remove the manual locks — but the slide nuance is that they only perform better under high contention.",
},
{
  id: "col-11", cat: "Collections & Generics", type: "mc",
  q: "Array vs List<T> — the core trade-off?",
  choices: [
    "Array is fixed-size with less memory overhead; List resizes dynamically and has a rich API (Add, Insert, RemoveAll, ...)",
    "Arrays are reference types, Lists are value types",
    "Lists are always faster",
    "Arrays cannot hold custom types",
  ],
  answer: 0,
  explain: "An array's size cannot change after initialization; the slides motivate List<T> by showing the manual grow-an-array dance (allocate bigger, copy everything, append) that List<T>.Add automates. Default to List when the count changes at runtime; use arrays when the size is known and constant.",
},
{
  id: "col-12", cat: "Collections & Generics", type: "short",
  q: "You're building undo/redo for an editor. Which collection, and why?",
  answer: "Stack<T> — LIFO matches undo semantics: Push each action as it is performed, Pop to undo the most recent one first. A second stack holds undone actions for redo (Pop from undo, Push to redo, and back). Queues are the opposite: FIFO, suited to task scheduling and print jobs.",
  explain: "Undo/redo and navigation history are the slides' own Stack use cases — the newest action must come back first, which is exactly last-in-first-out. Mentioning the two-stack redo design shows you understand the data structure rather than just naming it.",
},
{
  id: "col-13", cat: "Collections & Generics", type: "code-mc",
  q: "What output will the following code produce?",
  code: "Stack<int> s = new();\ns.Push(1);\ns.Push(2);\ns.Push(3);\n\nConsole.Write(s.Pop());\nConsole.Write(s.Pop());",
  choices: [
    "32",
    "12",
    "13",
    "21",
  ],
  answer: 0,
  explain: "A Stack is LIFO (last-in, first-out): Push adds to the top, Pop removes from the top. After pushing 1, 2, 3 the top is 3, so the first Pop returns 3 and the second returns 2 — written with no separator that prints \"32\". A Queue would be FIFO and print \"12\" (1 then 2). Peek would read the top without removing it. Stacks model undo/redo and call/navigation history.",
},

/* ==================== LINQ & JSON · attribute / serialization additions ==================== */

{
  id: "lnq-16", cat: "LINQ & JSON", type: "code-mc",
  q: "The JSON file uses \"ship_name\", but your model property is ShipName. Which attribute maps them, and where does it go?",
  code: "public class Ship\n{\n    [JsonPropertyName(\"ship_name\")]\n    public string ShipName { get; set; }\n}",
  choices: [
    "[JsonPropertyName(\"ship_name\")] on the property — System.Text.Json then reads/writes that exact key",
    "[JsonProperty(\"ship_name\")] — the Newtonsoft attribute is the one System.Text.Json uses",
    "[JsonKey(\"ship_name\")] on the property",
    "Nothing — System.Text.Json matches snake_case to PascalCase automatically",
  ],
  answer: 0,
  explain: "[JsonPropertyName(\"...\")] from System.Text.Json.Serialization sets the exact JSON key for one property, in both directions. It is the right tool when the file's keys do not match your C# names — and the exam's strict \"use these EXACT key names\" output requirement is solved the same way (or via an anonymous type whose member names ARE the keys). [JsonProperty] is Newtonsoft and is not on the allowed-namespace list.",
},
{
  id: "lnq-17", cat: "LINQ & JSON", type: "code-mc",
  q: "What does [JsonIgnore] do to PasswordHash during serialization?",
  code: "public class User\n{\n    public string Name { get; set; }\n\n    [JsonIgnore]\n    public string PasswordHash { get; set; }\n}",
  choices: [
    "PasswordHash is omitted from the produced JSON (and skipped when reading)",
    "PasswordHash is written but with a null value",
    "It throws unless PasswordHash is also nullable",
    "It renames PasswordHash to \"ignore\" in the output",
  ],
  answer: 0,
  explain: "[JsonIgnore] (System.Text.Json.Serialization) drops the member from serialization entirely; by default it is also skipped on deserialize. Use it for computed, secret, or framework-only properties you never want in the file. The conditional form [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] omits a property only when it is null — handy for the exam's optional fields.",
},
{
  id: "lnq-18", cat: "LINQ & JSON", type: "mc",
  q: "Which JsonSerializerOptions setting lets you READ a file whose keys are camelCase into PascalCase C# properties without any per-property attributes?",
  choices: [
    "PropertyNameCaseInsensitive = true",
    "WriteIndented = true",
    "PropertyNamingPolicy = JsonNamingPolicy.CamelCase",
    "[JsonPropertyName] on every property",
  ],
  answer: 0,
  explain: "PropertyNameCaseInsensitive = true makes deserialization tolerant of case, so \"shipName\" binds to ShipName. PropertyNamingPolicy = CamelCase affects how names are WRITTEN (and matched) — set it when you must EMIT camelCase keys. WriteIndented only pretty-prints. Know which knob solves reading-someone-else's-JSON versus producing-required-output.",
},
{
  id: "lnq-19", cat: "LINQ & JSON", type: "code-mc",
  q: "What does this LINQ query return?",
  code: "var result = products\n    .GroupBy(p => p.Category)\n    .Select(g => new\n    {\n        Category = g.Key,\n        AveragePrice = g.Where(p => p.Price < 100)\n                        .Select(p => p.Price)\n                        .DefaultIfEmpty(0)\n                        .Average()\n    })\n    .OrderByDescending(x => x.AveragePrice)\n    .First();",
  choices: [
    "The category with the highest average price among its products costing less than 100",
    "The category containing the single highest individual product price under 100",
    "The category whose under-100 products have the highest total (summed) price",
    "The category with the highest average price across ALL its products",
  ],
  answer: 0,
  explain: "Read it per group from the inside out: within each category keep only products priced < 100, take their prices, and Average them — DefaultIfEmpty(0) makes a category with no cheap product average 0 instead of throwing on an empty sequence. That yields one {Category, AveragePrice} per group; OrderByDescending then First pick the group with the biggest such average. It is an AVERAGE of the under-100 subset — not a sum, not a single max, not all products.",
},

/* ==================== OOP · records / extension / delegate additions ==================== */

{
  id: "oop-16", cat: "OOP", type: "code-mc",
  q: "What is the practical difference between these two declarations?",
  code: "public record PointR(int X, int Y);\npublic class  PointC { public int X; public int Y; }",
  choices: [
    "The record gets value-based Equals/GetHashCode/ToString and init-only positional props for free; the class compares by reference",
    "They compile to identical IL — record is only a naming convention",
    "The record is mutable, the class is immutable",
    "Records cannot be used as DTOs for JSON",
  ],
  answer: 0,
  explain: "A positional record auto-generates value equality (two records with equal members are Equal), a readable ToString, and init-only properties, plus a 'with' expression for non-destructive copies. A class compares by reference unless you override Equals. Use records for immutable data/DTOs (great for JSON models); use classes for entities with identity and mutable behavior.",
},
{
  id: "oop-17", cat: "OOP", type: "mc",
  q: "You have 'record Recipe(string Name, int Servings)' and need a copy with Servings = 4. What is the idiomatic move?",
  choices: [
    "var bigger = original with { Servings = 4 }; — the 'with' expression copies and overrides chosen members",
    "original.Servings = 4; — positional record props have public setters",
    "new Recipe(original) { Servings = 4 }; — records have a copy constructor you call directly",
    "Recipe.Clone(original, servings: 4);",
  ],
  answer: 0,
  explain: "The 'with' expression makes a non-destructive copy: it clones the record and replaces only the named members, leaving the original untouched. Positional record properties are init-only, so you cannot assign them after construction — that immutability is exactly why records pair so well with 'with'. This is a frequent records-vs-classes exam discriminator.",
},
{
  id: "oop-18", cat: "OOP", type: "code-mc",
  q: "Why does \"hello\".Shout() compile here?",
  code: "public static class StringExtensions\n{\n    public static string Shout(this string s) => s.ToUpper() + \"!\";\n}\n\n// elsewhere:\nstring loud = \"hello\".Shout();",
  choices: [
    "Shout is an extension method — a static method whose first parameter has 'this', callable as if it were an instance method on string",
    "string was reopened and a real Shout instance method was added to it",
    "It does not compile — you cannot add methods to sealed BCL types",
    "Shout works only because string is special-cased by the compiler",
  ],
  answer: 0,
  explain: "An extension method lives in a static class, is itself static, and marks its first parameter with 'this Type'; the compiler rewrites x.Shout() into StringExtensions.Shout(x). It adds syntax-level methods to types you cannot edit (string, IEnumerable) without inheritance — every LINQ operator (Where, Select) is exactly this pattern over IEnumerable<T>.",
},
{
  id: "oop-19", cat: "OOP", type: "mc",
  q: "Where must an extension method be defined for the 'this'-parameter syntax to be legal?",
  choices: [
    "In a non-generic, non-nested static class, as a static method whose first parameter is 'this T x'",
    "In any class, as long as the method is public",
    "Inside the type being extended, as a partial method",
    "In an interface as a default method",
  ],
  answer: 0,
  explain: "The three hard requirements: a top-level static class, a static method, and 'this' on the first parameter. The method is then in scope wherever its namespace is imported. Extension methods cannot access private members of the extended type — they are ordinary static methods with nicer call syntax, used heavily for fluent, readable pipelines.",
},
{
  id: "oop-20", cat: "OOP", type: "mc",
  q: "A base class defines a method; several subclasses override it and each behaves differently when the method is called through a base-class reference at runtime. Which OO principle enables this?",
  choices: [
    "Polymorphism",
    "Inheritance",
    "Encapsulation",
    "Abstraction",
  ],
  answer: 0,
  explain: "Polymorphism (\"many forms\") lets one call site run different behavior depending on the object's runtime type. The C# enabler is a 'virtual' base method that each subclass 'override's, so 'Animal a = new Dog(); a.Speak();' runs Dog's version. Inheritance only supplies the hierarchy; polymorphism is what makes the overridden method win at runtime. Use 'override' — 'new' merely hides and resolves by the variable's static type instead.",
},
{
  id: "oop-21", cat: "OOP", type: "mc",
  q: "What is the key difference between a delegate and an event in C#?",
  choices: [
    "A delegate can be invoked by any code that can reach it; an event can only be raised by the class that declares it (outsiders may only += / -=)",
    "They are identical; 'event' is purely syntactic sugar with no behavioral difference",
    "Events support multiple subscribers while delegates support only one",
    "Delegates can return values but events can only wrap void methods",
  ],
  answer: 0,
  explain: "An event is a restricted wrapper around a delegate: outside code may only subscribe (+=) or unsubscribe (-=), while only the declaring type can actually fire it (OnClick?.Invoke(...)). A bare public delegate field, by contrast, lets any caller overwrite the list or invoke it directly — that broken encapsulation is exactly what 'event' prevents. Multicast and void-ness are NOT the distinction; both can multicast.",
},

/* ==================== Threading & Async · delegate / primitive additions ==================== */

{
  id: "thr-15", cat: "Threading & Async", type: "code-mc",
  q: "What is the difference between these two delegate types?",
  code: "Func<int, int>  square = x => x * x;     // returns a value\nAction<string>  print  = s => Console.WriteLine(s);  // returns void",
  choices: [
    "Func<...,TResult> always returns its LAST type parameter; Action<...> returns void (no result)",
    "Func takes no arguments, Action takes one",
    "Func is async, Action is synchronous",
    "They are interchangeable — both can return a value",
  ],
  answer: 0,
  explain: "In Func<T1,...,TResult> the FINAL type argument is the return type, so Func<int,int> takes an int and returns an int; Func<int> takes nothing and returns an int. Action<...> returns nothing. Predicate<T> is a specialized Func<T,bool>. These are how a method receives behavior as a parameter — the lightweight Strategy pattern (e.g. list.Where(predicate)).",
},
{
  id: "thr-16", cat: "Threading & Async", type: "mc",
  q: "You need a delegate that takes a Recipe and returns a bool. Which BCL delegate fits with the least ceremony?",
  choices: [
    "Func<Recipe, bool> (or its alias Predicate<Recipe>)",
    "Action<Recipe>",
    "Func<bool, Recipe>",
    "Action<Recipe, bool>",
  ],
  answer: 0,
  explain: "Returning a value means Func, with the return type LAST: Func<Recipe, bool>. Predicate<Recipe> is the purpose-built alias for exactly 'takes one T, returns bool' and reads well in filtering APIs. Action<Recipe> is wrong because it returns void, and Func<bool, Recipe> has the arguments reversed.",
},
{
  id: "thr-17", cat: "Threading & Async", type: "code-mc",
  q: "Both versions make 10 tasks each add 1000. One is correct without a lock. Which, and why?",
  code: "// A:  counter++;                       inside each task\n// B:  Interlocked.Increment(ref counter); inside each task",
  choices: [
    "B — Interlocked.Increment is a single atomic hardware operation, so no lock is needed and no update is lost",
    "A — counter++ is atomic for int",
    "Both are fine; int writes are always atomic",
    "Neither — you must use a lock in both cases",
  ],
  answer: 0,
  explain: "counter++ is read-modify-write (three steps) and races; Interlocked.Increment(ref counter) performs the whole increment atomically in one instruction, so concurrent threads never clobber each other and the total is exact — no lock, less contention. Interlocked covers Increment/Decrement/Add/Exchange/CompareExchange; reach for lock only when the critical section is more than a single such operation.",
},
{
  id: "thr-18", cat: "Threading & Async", type: "mc",
  q: "When should you prefer Interlocked over a lock, and when must you fall back to lock?",
  choices: [
    "Interlocked for a single atomic counter/swap; lock when several operations must happen together as one unit",
    "Interlocked is always faster and fully replaces lock",
    "lock for counters; Interlocked for everything else",
    "They are identical in behavior and performance",
  ],
  answer: 0,
  explain: "Interlocked is a lock-free atomic for ONE operation (increment a shared count, swap a reference) — cheaper than a lock because no thread blocks. But when an invariant spans multiple steps (read A, decide, write B), only a lock makes the whole sequence atomic. Match the tool to the granularity: single op → Interlocked; compound critical section → lock.",
},
{
  id: "thr-19", cat: "Threading & Async", type: "code-mc",
  q: "Why is the first signature a bug waiting to happen?",
  code: "// A:  public async void  DoWorkAsync() { await Task.Delay(100); throw new Exception(); }\n// B:  public async Task   DoWorkAsync() { await Task.Delay(100); throw new Exception(); }",
  choices: [
    "A returns void, so it cannot be awaited and its exception escapes to crash the process; B returns Task, so callers can await and catch it",
    "A is fine; async void is the recommended default",
    "B leaks a thread because Task is never disposed",
    "They behave identically — return type does not matter for async",
  ],
  answer: 0,
  explain: "An async void method has no Task to await, so a caller cannot observe completion OR catch its exception — the throw is raised on the synchronization context and typically tears down the app. async Task is the rule everywhere; async void is reserved for event handlers whose signature is fixed (the Avalonia file-dialog Click handler is the sanctioned case). Commands should be async Task so [RelayCommand] makes an AsyncRelayCommand.",
},

/* ==================== Avalonia & MVVM · binding / timer / selection additions ==================== */

{
  id: "av-17", cat: "Avalonia & MVVM", type: "code-mc",
  q: "What does adding x:DataType to this Window root buy you?",
  code: "<Window xmlns=\"https://github.com/avaloniaui\"\n        xmlns:x=\"http://schemas.microsoft.com/winfx/2006/xaml\"\n        xmlns:vm=\"using:MyApp.ViewModels\"\n        x:DataType=\"vm:MainWindowViewModel\">\n  <TextBlock Text=\"{Binding Title}\"/>\n</Window>",
  choices: [
    "Compiled bindings — {Binding Title} is type-checked against MainWindowViewModel at build time, so a typo is a compile error and bindings are faster",
    "It sets the runtime DataContext to a new MainWindowViewModel",
    "It forces every binding inside to TwoWay",
    "It enables the XAML hot-reload previewer only",
  ],
  answer: 0,
  explain: "x:DataType turns on compiled bindings (the exam templates set AvaloniaUseCompiledBindingsByDefault), so binding paths resolve against the named type at compile time — a misspelled property fails the BUILD instead of silently doing nothing at runtime, which is a lifesaver under exam pressure. Inside a DataTemplate that binds a different model, declare x:DataType there too (the Canvas ContentPresenter style needs its own).",
},
{
  id: "av-18", cat: "Avalonia & MVVM", type: "mc",
  q: "Compiled bindings are on (x:DataType set). You bind {Binding Titel} but the property is Title. What happens?",
  choices: [
    "The project fails to compile — the misspelled path is caught at build time",
    "It binds to null silently and the control stays blank at runtime",
    "Avalonia auto-corrects to the nearest property name",
    "A runtime binding exception is thrown when the window opens",
  ],
  answer: 0,
  explain: "That is the whole point of compiled bindings: the path is validated against x:DataType at build time, so a typo is a compiler error you fix immediately rather than a blank control you debug later. Classic (reflection) bindings would just fail silently at runtime. Enable them with x:DataType on the root (and again inside any DataTemplate binding a different type).",
},
{
  id: "av-19", cat: "Avalonia & MVVM", type: "code-mc",
  q: "Both keep a counter ticking +1 every 100 ms. Which respects \"all UI updates on the UI thread\" by construction?",
  code: "// A: var t = new DispatcherTimer { Interval = TimeSpan.FromMilliseconds(100) };\n//    t.Tick += (_, _) => Count++;  t.Start();\n// B: Task.Run(async () => { while (run) { await Task.Delay(100); Count++; } });",
  choices: [
    "A — DispatcherTimer.Tick fires on the UI thread, so mutating the bound Count there is safe without any marshalling",
    "B — Task.Run already runs on the UI thread",
    "Both are equally safe; the timer source does not matter",
    "Neither — counters must always use Interlocked",
  ],
  answer: 0,
  explain: "DispatcherTimer raises Tick on the UI thread, so updating bound properties in the handler is safe by construction — ideal for the re-exam counter. A bare Task.Run loop runs on a thread-pool thread, so writing Count from it touches the UI off-thread; you would need Dispatcher.UIThread.Post(() => Count++) (or PeriodicTimer with care). Choose DispatcherTimer when you simply need a UI-thread heartbeat.",
},
{
  id: "av-20", cat: "Avalonia & MVVM", type: "mc",
  q: "The exam wants the user to pick SEVERAL items in a ListBox at once. What must change from the single-select default?",
  choices: [
    "Set SelectionMode=\"Multiple\" and bind/read SelectedItems (a collection) — SelectedItem only ever holds one",
    "Bind SelectedItem to a List<T>",
    "Set IsMultiSelect=\"True\" on the ViewModel",
    "Nothing — ListBox is multi-select by default",
  ],
  answer: 0,
  explain: "ListBox defaults to single selection; SelectionMode=\"Multiple\" (or \"Toggle\"/\"Multiple\") enables picking many rows. SelectedItem holds just one item, so for several you use the SelectedItems collection. The re-exam highlighted a selected recipe's ingredients via the single-select OnSelectedRecipeChanged hook; multi-select instead reads SelectedItems. Know both the property and the mode.",
},
{
  id: "av-21", cat: "Avalonia & MVVM", type: "code-mc",
  q: "The button should disable itself when Count is 0. Why is THIS wiring incomplete?",
  code: "[ObservableProperty]\nprivate int _count;\n\n[RelayCommand(CanExecute = nameof(CanDecrement))]\nprivate void Decrement() => Count--;\n\nprivate bool CanDecrement() => Count > 0;",
  choices: [
    "Nothing re-queries CanDecrement when Count changes — add [NotifyCanExecuteChangedFor(nameof(DecrementCommand))] on _count (or call DecrementCommand.NotifyCanExecuteChanged())",
    "CanExecute must point to a property, not a method",
    "The command body must be async for CanExecute to work",
    "CanDecrement must be public",
  ],
  answer: 0,
  explain: "[RelayCommand(CanExecute = nameof(CanDecrement))] wires the guard, but the UI only re-evaluates it when CanExecuteChanged fires — which does not happen automatically when Count changes. Annotate the field with [NotifyCanExecuteChangedFor(nameof(DecrementCommand))] so the generated setter raises it, or call DecrementCommand.NotifyCanExecuteChanged() in OnCountChanged. This exact gap was the June Counter trap.",
},

/* ==================== "Write the code" shorts ==================== */

{
  id: "sol-17", cat: "SOLID", type: "short",
  q: "A Service news up its own FileLogger inside its constructor. Write the DIP fix (constructor-inject an abstraction) and one line showing the composition root wiring it.",
  answer: "public class Service\n{\n    private readonly ILogger _log;\n    public Service(ILogger log) => _log = log;   // depend on the abstraction, injected in\n}\n\n// composition root (Program.cs / App.axaml.cs) — the ONLY place that knows the concrete type:\nvar service = new Service(new FileLogger());",
  explain: "DIP: \"depend upon abstractions, not concretions.\" Replace the in-constructor 'new FileLogger()' with an ILogger constructor parameter and let the composition root choose and inject the concrete logger. Benefits to state: implementations become swappable without touching Service, and tests can inject a fake ILogger. New-ing BCL types (List<T>) inside is fine; new-ing your own services in business logic is the smell.",
},
{
  id: "lnq-17s", cat: "LINQ & JSON", type: "short",
  q: "Write the LINQ: average number of ingredients per recipe Category, projected as { Category, AvgIngredients }, ordered with the highest average first.",
  answer: "var byCategory = recipes\n    .GroupBy(r => r.Category)\n    .Select(g => new\n    {\n        Category = g.Key,\n        AvgIngredients = g.Average(r => r.Ingredients?.Count ?? 0)\n    })\n    .OrderByDescending(x => x.AvgIngredients)\n    .ToList();",
  explain: "The exam's signature shape: GroupBy yields groups exposing .Key (the Category) and the group's items; Select projects key + an aggregate (Average/Sum/Count); OrderByDescending puts the biggest on top. Guard the inner count with ?.Count ?? 0 for recipes whose Ingredients list is missing in the JSON. The June exam wanted exactly this for average trips per ship type.",
},
{
  id: "lnq-18s", cat: "LINQ & JSON", type: "short",
  q: "Write the call that serializes 'results' to a human-readable JSON string with camelCase property names.",
  answer: "var options = new JsonSerializerOptions\n{\n    WriteIndented = true,\n    PropertyNamingPolicy = JsonNamingPolicy.CamelCase\n};\nstring json = JsonSerializer.Serialize(results, options);\nFile.WriteAllText(\"results.json\", json);",
  explain: "WriteIndented = true pretty-prints with newlines/indentation (the re-exam wanted readable output); PropertyNamingPolicy = JsonNamingPolicy.CamelCase emits camelCase keys. Serialize infers the type from the object, so no <T> needed. When the spec demands EXACT key names, prefer an anonymous type whose member names ARE the keys (or [JsonPropertyName]) over a naming policy.",
},
{
  id: "av-22", cat: "Avalonia & MVVM", type: "short",
  q: "Write a [RelayCommand] Decrement that is disabled when Count is 0, including the wiring that re-enables the button when Count changes.",
  answer: "[ObservableProperty]\n[NotifyCanExecuteChangedFor(nameof(DecrementCommand))]\nprivate int _count;\n\n[RelayCommand(CanExecute = nameof(CanDecrement))]\nprivate void Decrement() => Count--;     // use the generated property, not the field\n\nprivate bool CanDecrement() => Count > 0;",
  explain: "[RelayCommand(CanExecute = nameof(CanDecrement))] generates DecrementCommand whose enabled state follows the guard. The button only re-checks when CanExecuteChanged fires, so [NotifyCanExecuteChangedFor(nameof(DecrementCommand))] on _count makes the generated setter raise it on every change. Mutate via Count-- (the property) so the setter runs; writing _count would skip the notification. This was the June Counter task.",
},
{
  id: "av-23", cat: "Avalonia & MVVM", type: "mc",
  q: "What is the core difference between MVC and MVVM?",
  choices: [
    "MVC uses a Controller to mediate between View and Model; MVVM uses a ViewModel the View talks to through data binding and commands",
    "In MVVM the Model no longer contains any business logic",
    "MVVM is just a newer name for MVC with an identical structure",
    "MVC keeps state in the Controller while MVVM keeps state in the Model",
  ],
  answer: 0,
  explain: "Both decouple presentation from data, but the middle layer differs. MVC's Controller actively pushes updates between View and Model. MVVM replaces it with a ViewModel the View BINDS to: the link is data bindings (View→VM), notifications via INotifyPropertyChanged (VM→View) and Commands — so the ViewModel never references controls and the View holds no logic. The Model still owns domain logic in both; state is not forced into the Model.",
},
{
  id: "tst-13", cat: "Testing", type: "short",
  q: "Write the minimal skeleton for a headless Avalonia UI test: the assembly attribute, the app builder, and one [AvaloniaFact] that shows the window.",
  answer: "// once per test assembly:\n[assembly: AvaloniaTestApplication(typeof(TestAppBuilder))]\n\npublic class TestAppBuilder\n{\n    public static AppBuilder BuildAvaloniaApp() =>\n        AppBuilder.Configure<App>().UseHeadless(new AvaloniaHeadlessPlatformOptions());\n}\n\npublic class MainWindowTests\n{\n    [AvaloniaFact]\n    public void Window_Opens_WithViewModel()\n    {\n        var window = new MainWindow { DataContext = new MainWindowViewModel() };\n        window.Show();   // required even though nothing renders\n        Assert.NotNull(window.FindControl<Button>(\"IncrementButton\"));\n    }\n}",
  explain: "The three mandatory pieces: assembly-level [AvaloniaTestApplication(typeof(TestAppBuilder))] with a builder calling UseHeadless; [AvaloniaFact] (never [Fact]) so the test runs on Avalonia's dispatcher; and window.Show() before interacting. FindControl needs a Name=\"...\" in the AXAML or it returns null. Add the Avalonia.Headless.XUnit 11.2.1 package at home so it restores offline.",
},
{
  id: "thr-20", cat: "Threading & Async", type: "short",
  q: "Write the +1-every-100ms async counter loop that updates a bound Count safely and stops on cancellation (Task.Delay form).",
  answer: "private CancellationTokenSource? _cts;\n\nprivate async Task RunAsync()\n{\n    _cts = new CancellationTokenSource();\n    try\n    {\n        while (!_cts.Token.IsCancellationRequested)\n        {\n            await Task.Delay(100, _cts.Token);\n            // marshal the UI-bound write back to the UI thread:\n            Dispatcher.UIThread.Post(() => Count++);\n        }\n    }\n    catch (OperationCanceledException) { /* normal Stop — not an error */ }\n}\n\nprivate void Stop() => _cts?.Cancel();",
  explain: "Pass the token into Task.Delay so Stop() (via _cts.Cancel()) breaks the loop promptly; catch OperationCanceledException as the NORMAL shutdown, not a failure. Because the loop runs off the UI thread, marshal Count++ with Dispatcher.UIThread.Post. Simpler alternatives the re-exam accepts: a DispatcherTimer (Tick on the UI thread, no marshalling) or PeriodicTimer with WaitForNextTickAsync(token).",
},

/* ==================== Design Process (prototyping, iterative dev) ==================== */

{
  id: "proc-01", cat: "Design Process", type: "mc",
  q: "What type of prototype focuses on a single, complete user flow (for example, purchasing one specific product) while leaving other functionality unimplemented or ignored?",
  choices: [
    "Vertical prototype",
    "Horizontal prototype",
    "Low-fidelity prototype",
    "Wireframe",
  ],
  answer: 0,
  explain: "A vertical prototype builds ONE item end-to-end — its sub-views, logic and data — and leaves the others unimplemented, so you prove a single complete journey works. A horizontal prototype is the opposite: a thin base view across many items, broad but shallow. Vertical vs horizontal is Lecture 7's RESOLUTION axis (how much functionality is built); fidelity — how polished it LOOKS — is a separate axis, so 'low-fidelity' and 'wireframe' are not the answer.",
},
{
  id: "proc-02", cat: "Design Process", type: "mc",
  q: "A prototype shows every screen and the full navigation of an app, but no button actually does anything yet. Which kind is it?",
  choices: [
    "Horizontal prototype",
    "Vertical prototype",
    "High-fidelity prototype",
    "Evolutionary prototype",
  ],
  answer: 0,
  explain: "Horizontal = wide and shallow: a single base view for each menu item, so people can react to scope and navigation, but little or no logic works underneath. A vertical prototype would instead build one item in full depth. Broad-vs-deep is Lecture 7's RESOLUTION axis (how much functionality is implemented); it is independent of fidelity, which is how close the prototype LOOKS to the finished app.",
},
{
  id: "proc-03", cat: "Design Process", type: "mc",
  q: "What best describes a low-fidelity prototype?",
  choices: [
    "A rough sketch or wireframe — boxes and labels, no real styling or data — made cheaply and early for fast feedback",
    "A pixel-perfect, interactive build using real colors, fonts and data",
    "The final product shipped to a small group of users",
    "A prototype that gradually becomes the production system",
  ],
  answer: 0,
  explain: "Low-fidelity prototypes (paper sketches, wireframes) are deliberately rough, so they are quick and cheap to make and easy to discard — and so reviewers critique layout and flow instead of mistaking them for a finished product. High-fidelity prototypes look and behave close to the real thing (real styling, realistic data, clickable), cost more, and come later for usability testing and sign-off.",
},
{
  id: "proc-04", cat: "Design Process", type: "mc",
  q: "When is a high-fidelity prototype most appropriate?",
  choices: [
    "Later, for usability testing and stakeholder sign-off, when you need something close to the real look and feel",
    "At the very start, to brainstorm rough layout ideas as cheaply as possible",
    "Only when you intend to throw the prototype away immediately",
    "Whenever you want to avoid writing any UI code at all",
  ],
  answer: 0,
  explain: "High-fidelity prototypes use real colors, typography and data and are often interactive, so they are expensive to produce — you invest in them once the direction is settled, to validate detailed usability and get confident sign-off. Early exploration is better served by cheap low-fidelity sketches and wireframes that invite honest critique and are painless to discard or redo.",
},
{
  id: "proc-05", cat: "Design Process", type: "mc",
  q: "What is the difference between a wireframe and a mockup?",
  choices: [
    "A wireframe is a low-fidelity structural blueprint (placement, hierarchy, no styling); a mockup is a higher-fidelity static visual of the finished look",
    "A wireframe is interactive; a mockup is always made of paper",
    "They are two words for exactly the same artifact",
    "A mockup has no colors, while a wireframe is fully styled",
  ],
  answer: 0,
  explain: "A wireframe answers 'what goes where' — boxes, labels and hierarchy with no final color or content. A mockup is a static but high-fidelity picture of the finished visual design (real colors, fonts, imagery) that you cannot click. Add interaction and you have a prototype. The progression from low to high fidelity is roughly wireframe, then mockup, then interactive prototype.",
},
{
  id: "proc-06", cat: "Design Process", type: "mc",
  q: "In Lecture 7's prototyping framework, what does the RESOLUTION of a prototype describe?",
  choices: [
    "How much of the final application's functionality is already implemented",
    "How visually close the prototype is to the finished application",
    "How many screens the prototype contains in total",
    "Which UI framework the prototype is built with",
  ],
  answer: 0,
  explain: "Lecture 7 separates two axes. RESOLUTION is how much of the final app's functionality is actually implemented; FIDELITY is how close it LOOKS to the finished product. Vertical and horizontal prototypes are the resolution differentiation — vertical builds one item deeply, horizontal spreads a thin base view across many items. Mixing up resolution (how much works) with fidelity (how it looks) is the classic trap.",
},
{
  id: "proc-07", cat: "Design Process", type: "mc",
  q: "What is the primary purpose of building a prototype?",
  choices: [
    "To explore ideas and validate requirements and UX with feedback BEFORE committing to the full build, reducing risk",
    "To ship a finished, fully featured product to customers as fast as possible",
    "To replace writing any specification or design documents",
    "To permanently lock the design so it can no longer change",
  ],
  answer: 0,
  explain: "Prototypes exist to learn cheaply: they surface misunderstandings in requirements and usability problems early, when changes are still cheap, rather than after the whole system is built. They are partial and provisional by design. That risk-reduction goal is why rough, low-fidelity prototypes are common early on — the point is feedback, not a deliverable.",
},
{
  id: "proc-08", cat: "Design Process", type: "mc",
  q: "A clickable mockup uses the real colors, fonts and layout of the finished app, but almost none of its buttons actually do anything. In Lecture 7's terms, how is it classified?",
  choices: [
    "High fidelity, low resolution",
    "Low fidelity, high resolution",
    "High fidelity, high resolution",
    "Low fidelity, low resolution",
  ],
  answer: 0,
  explain: "Fidelity is how close it LOOKS to the final app; resolution is how much functionality actually works. Real colors, fonts and layout make it high fidelity, but if the buttons do nothing the resolution is low. The opposite case is a rough paper prototype you operate by hand to fake a whole flow: low fidelity, higher resolution. A functional prototype scores high on both axes.",
},
{
  id: "proc-09", cat: "Design Process", type: "mc",
  q: "Lecture 7 lists several prototyping artifacts. Which is the LOWEST-fidelity one?",
  choices: [
    "A hand-drawn sketch of the screen",
    "A high-fidelity mockup",
    "A functional prototype",
    "A pixel-accurate interactive demo",
  ],
  answer: 0,
  explain: "Lecture 7's artifacts run low to high fidelity: sketches, wireframes and paper prototypes are the low-fidelity end (rough boxes and labels, no styling); mockups and functional prototypes are the high-fidelity end (real colors, fonts, data). A sketch is the cheapest, roughest, earliest form, used deliberately so reviewers suggest big changes before real effort is sunk in.",
},
{
  id: "proc-10", cat: "Design Process", type: "mc",
  q: "Why are paper or low-fidelity prototypes especially useful in the EARLY stages of design?",
  choices: [
    "They are cheap and fast to make and easy to change, so people critique the ideas freely instead of treating them as final",
    "They are indistinguishable from the finished product, so they need no further work",
    "They guarantee the design will never need to change again",
    "They require the full tech stack and real data to be in place first",
  ],
  answer: 0,
  explain: "Because a sketch costs minutes and obviously is not 'done', reviewers feel free to suggest big changes and you can redraw instantly — feedback is honest and iteration is painless. A polished high-fidelity prototype invites nitpicking about colors and can make stakeholders reluctant to request structural changes. Cheap-and-rough is exactly what early, high-uncertainty exploration wants.",
},

/* ==================== EXAM THEORY (Problem 1: OOP / SOLID / design patterns) ====================
   Snippet-driven multiple choice that rehearses exactly what Problem 1 grades:
   name the principle/pattern from real exam-style code, including the planted
   violations. Snippets are adapted from the AquariumRescueCenter and ToolLibrary
   mock-exam projects. Drill this category alone to practise the written analysis. */

{
  id: "th-01", cat: "Exam Theory", type: "mc",
  q: "Problem 1 (1.3) asks you to cover the four basic OOP principles. Which set is the one to write about?",
  choices: [
    "Encapsulation, Inheritance, Abstraction, Polymorphism",
    "Single Responsibility, Open/Closed, Liskov, Dependency Inversion",
    "Creational, Structural, Behavioural, Concurrency",
    "Cohesion, Coupling, Composition, Delegation",
  ],
  answer: 0,
  explain: "The four OOP pillars graded in 1.3 are Encapsulation (hiding state), Inheritance (a subclass reusing and extending a base), Abstraction (exposing WHAT a type does while hiding HOW, via abstract classes and interfaces) and Polymorphism (one call, type-specific behaviour). The SRP/OCP/LSP/ISP/DIP list is SOLID, which is the separate question 1.4.",
},
{
  id: "th-02", cat: "Exam Theory", type: "code-mc",
  q: "Which OOP principle does this best demonstrate, and what evidence would you cite?",
  code: "public abstract class RescueAnimal\n{\n    private int _severity;\n    public int Severity => _severity;\n\n    public void EaseSeverity(int amount)\n    {\n        if (amount < 0) throw new ArgumentOutOfRangeException(nameof(amount));\n        _severity = Math.Max(0, _severity - amount);\n    }\n}",
  choices: [
    "Encapsulation — _severity is private and only changes through a validating method",
    "Inheritance — the class can be subclassed",
    "Polymorphism — Severity can be overridden",
    "Abstraction — the class is abstract",
  ],
  answer: 0,
  explain: "Encapsulation is information hiding: the field _severity is private and exposed only through a read-only Severity property, and the one mutator EaseSeverity validates its input and clamps the result. Cite 'private field plus controlled, validating mutation' as your evidence. That the class is also abstract is true but answers Abstraction, a different pillar.",
},
{
  id: "th-03", cat: "Exam Theory", type: "code-mc",
  q: "What does marking RescueAnimal `abstract` accomplish?",
  code: "public abstract class RescueAnimal : IRescueAnimal\n{\n    public virtual string DescribeCondition()\n        => $\"{Name} (severity {Severity}/10)\";\n}\n\n// var a = new RescueAnimal(...);  // does this compile?",
  choices: [
    "It cannot be instantiated directly — only concrete subclasses like SeaTurtle can; it defines a shared abstraction",
    "It makes every member virtual automatically",
    "It prevents the class from implementing interfaces",
    "It makes the class a singleton",
  ],
  answer: 0,
  explain: "An abstract class cannot be instantiated (new RescueAnimal(...) is a compile error); it exists to be inherited and to define the shared shape and behaviour of its concrete subtypes. That is the Abstraction pillar: callers work with the general 'rescue animal' idea while concrete types fill in specifics. abstract does not auto-mark members virtual, nor block interfaces.",
},
{
  id: "th-04", cat: "Exam Theory", type: "code-mc",
  q: "Which principle is shown, and what does this print?",
  code: "public class RescueAnimal { public virtual string Describe() => \"animal\"; }\npublic sealed class SeaTurtle : RescueAnimal\n{\n    public override string Describe() => base.Describe() + \" [turtle]\";\n}\n\nRescueAnimal a = new SeaTurtle();\nConsole.WriteLine(a.Describe());",
  choices: [
    "Polymorphism — prints \"animal [turtle]\" via runtime override dispatch",
    "Encapsulation — prints \"animal\"",
    "Hiding — prints \"animal\" because the variable is RescueAnimal",
    "Compile error — base.Describe() is not allowed",
  ],
  answer: 0,
  explain: "virtual plus override gives runtime polymorphism: the call dispatches on the object's actual type (SeaTurtle), not the variable's declared type, so SeaTurtle.Describe runs and prints \"animal [turtle]\" (it calls base first). If SeaTurtle had used `new` instead of `override`, the declared type would win and it would print \"animal\" — that is hiding, which the slides say to avoid.",
},
{
  id: "th-05", cat: "Exam Theory", type: "code-mc",
  q: "What does this declaration tell you about C#'s type model?",
  code: "public sealed class SeaTurtle\n    : RescueAnimal, IAquatic, IAirBreathing\n{\n    /* ... */\n}",
  choices: [
    "A class has exactly one base class but may implement many interfaces",
    "A class may inherit from many base classes",
    "IAquatic and IAirBreathing must share a base interface",
    "sealed means it cannot implement interfaces",
  ],
  answer: 0,
  explain: "C# allows single class inheritance (RescueAnimal is the one base) but unlimited interface implementation, so SeaTurtle is also IAquatic and IAirBreathing. Inheritance models an 'is-a' reuse of a base; interfaces add 'can-do' capability contracts that even unrelated types can share. sealed only blocks further subclassing — it has no effect on implementing interfaces.",
},
{
  id: "th-06", cat: "Exam Theory", type: "code-mc",
  q: "Why split these into two small interfaces instead of one big IAnimalTraits?",
  code: "public interface IAquatic     { double PreferredWaterTempC { get; } }\npublic interface IAirBreathing { int MaxSubmergeMinutes { get; } }\n\n// MoonJellyfish : RescueAnimal, IAquatic               (no air)\n// SeaTurtle     : RescueAnimal, IAquatic, IAirBreathing",
  choices: [
    "Interface Segregation — a jellyfish needs no air-breathing members, so it should not be forced to implement them",
    "Single Responsibility — interfaces may only have one method",
    "Liskov Substitution — subtypes must be identical to their base",
    "It is purely stylistic and changes nothing",
  ],
  answer: 0,
  explain: "This is the Interface Segregation Principle: clients should not be forced to depend on members they do not use. A MoonJellyfish is aquatic but does not breathe air, so it implements IAquatic only; one fat combined interface would force a meaningless MaxSubmergeMinutes on it. Small, role-focused interfaces keep each type honest and are a clean SOLID point for 1.4.",
},
{
  id: "th-07", cat: "Exam Theory", type: "code-mc",
  q: "Which SOLID principle does this constructor demonstrate?",
  code: "public sealed class IntakeDesk\n{\n    private readonly IEventLog _log;\n    private readonly IStaffNotifier _notifier;\n\n    public IntakeDesk(IEventLog log, IStaffNotifier notifier)\n    {\n        _log = log;\n        _notifier = notifier;\n    }\n}",
  choices: [
    "Dependency Inversion — it depends on abstractions (IEventLog, IStaffNotifier) injected from outside",
    "Single Responsibility — it has only one constructor",
    "Open/Closed — the class is sealed",
    "Liskov Substitution — it takes interface parameters",
  ],
  answer: 0,
  explain: "Dependency Inversion: the high-level IntakeDesk depends on abstractions (IEventLog, IStaffNotifier) rather than concrete classes, and they are injected through the constructor. You can hand it a ConsoleEventLog, a file logger, or a silent test double with no change to the desk. Passing interfaces is the mechanism; 'depend on abstractions, not concretions' is the principle.",
},
{
  id: "th-08", cat: "Exam Theory", type: "code-mc",
  q: "The tank-picking algorithm is an interface injected into IntakeDesk and called through AssignTank. Which design pattern is this?",
  code: "public interface ITankAssignmentStrategy { string AssignTank(IRescueAnimal a); }\n\npublic sealed class IntakeDesk\n{\n    private readonly ITankAssignmentStrategy _assignment;\n    // ... calls _assignment.AssignTank(animal) to decide the tank\n}",
  choices: [
    "Strategy — a family of interchangeable algorithms behind one interface, chosen at runtime",
    "Singleton — one global instance",
    "Observer — subscribers react to events",
    "Factory — it creates objects",
  ],
  answer: 0,
  explain: "Strategy: a varying algorithm (how to assign a tank) is captured behind the ITankAssignmentStrategy interface, so different rules can be swapped in without changing IntakeDesk. The desk just calls AssignTank and lets the injected strategy decide. This also supports OCP — add a new assignment rule as a new class, with no edits to the desk itself.",
},
{
  id: "th-09", cat: "Exam Theory", type: "code-mc",
  q: "AdmitAnimal hides tank assignment, logging and treatments behind one simple call. Which pattern does IntakeDesk play?",
  code: "public void AdmitAnimal(IRescueAnimal animal, IEnumerable<ITreatment> treatments)\n{\n    string tank = _assignment.AssignTank(animal);\n    Residents.Add(animal);\n    _log.Record($\"Admitted {animal.Name} -> {tank}\");\n    foreach (var t in treatments) t.ApplyTo(animal);\n}",
  choices: [
    "Facade — one simple entry point over a more complex subsystem",
    "Adapter — it converts one interface into another",
    "Decorator — it wraps an object to add behaviour",
    "Template Method — subclasses fill in steps",
  ],
  answer: 0,
  explain: "Facade: IntakeDesk gives staff a single, simple operation (AdmitAnimal) that coordinates a more complex subsystem — the assignment strategy, the treatments and the logger — so callers never wire those pieces together themselves. Adapter converts between incompatible interfaces and Decorator adds behaviour by wrapping; neither matches 'one simple front door over many parts'.",
},
{
  id: "th-10", cat: "Exam Theory", type: "code-mc",
  q: "What is the flaw here, and which principle does it break?",
  code: "public sealed class IntakeDesk\n{\n    // any caller can do:  desk.Residents.Clear();  or  desk.Residents = new();\n    public List<IRescueAnimal> Residents = new();\n\n    public void AdmitAnimal(IRescueAnimal a, ...) { Residents.Add(a); /* ... */ }\n}",
  choices: [
    "Encapsulation — a public mutable field lets callers bypass AdmitAnimal and corrupt the resident list",
    "Nothing — public fields are best practice for lists",
    "Polymorphism — the list should be virtual",
    "Inheritance — IntakeDesk should be abstract",
  ],
  answer: 0,
  explain: "Public mutable state breaks encapsulation: because Residents is a public field, any caller can reassign or clear it, sidestepping AdmitAnimal and the invariants it maintains (logging, treatments, alerts). The fix is to make the list private and expose a read-only view (IReadOnlyList) with AdmitAnimal as the only way to add. In 1.4 cite this as a deliberate violation.",
},
{
  id: "th-11", cat: "Exam Theory", type: "code-mc",
  q: "_notifier is injected and stored but never used anywhere. How do you name this smell in your analysis?",
  code: "public IntakeDesk(ITankAssignmentStrategy a, IEventLog log, IStaffNotifier notifier)\n{\n    _assignment = a;\n    _log = log;\n    _notifier = notifier;   // stored...\n}\n// ...but no method ever calls _notifier.Notify(...)",
  choices: [
    "An unused injected dependency — the class declares a collaborator it never calls",
    "A textbook example of good encapsulation",
    "Polymorphism through interfaces",
    "A correct use of the Observer pattern",
  ],
  answer: 0,
  explain: "An injected dependency that is stored but never used is a dead dependency: it advertises a collaborator (and a feature — staff alerts) the class never exercises, so the specified behaviour is missing. It misleads readers and complicates construction. Flag it under SOLID as a design smell: the class depends on something it does not need; either use it or remove it.",
},
{
  id: "th-12", cat: "Exam Theory", type: "code-mc",
  q: "What is wrong here, and which principles does the downcast break?",
  code: "private readonly IToolCatalog _catalog;        // depends on the abstraction...\n\nvar inMemory = _catalog as InMemoryToolCatalog; // ...then downcasts to the concrete\nvar pick = inMemory.PickRandomTool(candidates); // a method NOT on IToolCatalog",
  choices: [
    "It breaks LSP and DIP — another valid IToolCatalog fails the cast, and the class secretly depends on a concrete type",
    "Nothing — downcasting is always safe",
    "It improves Open/Closed",
    "It is an example of good encapsulation",
  ],
  answer: 0,
  explain: "The service claims to depend on IToolCatalog but casts down to the concrete InMemoryToolCatalog to call PickRandomTool, which the interface does not declare. That breaks Liskov Substitution (a different valid IToolCatalog makes the cast null and silently disables the feature) and Dependency Inversion (it now depends on a concretion). Fix: put the needed method on the interface.",
},
{
  id: "th-13", cat: "Exam Theory", type: "code-mc",
  q: "A List<ILendingRule> is injected but IsSatisfiedBy is never called. What does this mean for Open/Closed?",
  code: "private readonly List<ILendingRule> _rules;   // injected and stored\n\npublic List<Tool> GenerateLoanList(MemberProfile m, int n)\n{\n    // ...builds the list but never calls _rules[i].IsSatisfiedBy(...)\n}",
  choices: [
    "The rule abstraction is dead — it suggests Open/Closed extensibility but delivers none, because the policy is never applied",
    "It perfectly implements Open/Closed",
    "It is a textbook Strategy pattern in action",
    "It demonstrates Liskov Substitution",
  ],
  answer: 0,
  explain: "OCP means extending behaviour by adding new classes rather than editing existing ones — the ILendingRule design WOULD allow that, but since GenerateLoanList never calls IsSatisfiedBy, no rule is ever enforced. The abstraction is dead weight: it looks extensible yet changes nothing. In your write-up, name the principle the code gestures at and explain precisely why it is not actually achieved.",
},
{
  id: "th-14", cat: "Exam Theory", type: "code-mc",
  q: "If the service actually used these rules, adding a new policy as a new class (without editing the service) would show which principle?",
  code: "public class CertificationRule : ILendingRule\n{ public bool IsSatisfiedBy(Tool t, MemberProfile m) => /* ... */; }\n\npublic class AdultOnlyRule : ILendingRule\n{ public bool IsSatisfiedBy(Tool t, MemberProfile m) => /* ... */; }\n// a new policy = a new class implementing ILendingRule",
  choices: [
    "Open/Closed — open to extension (new rule classes), closed to modification (no edits to existing code)",
    "Single Responsibility",
    "Encapsulation",
    "Inheritance",
  ],
  answer: 0,
  explain: "Open/Closed: each policy is its own ILendingRule class, so a new rule is added by writing a new class and registering it, never by editing the service or the other rules. That is 'open to extension, closed to modification'. It also leans on ISP (one tiny IsSatisfiedBy method) and on polymorphism to call them uniformly. The catch in this code is that the rules are never invoked.",
},
{
  id: "th-15", cat: "Exam Theory", type: "code-mc",
  q: "Which principle does this class best exemplify?",
  code: "public class SafetyBriefingGenerator\n{\n    public List<string> Generate(List<Tool> loanList)\n    {\n        // builds, deduplicates and sorts the briefing lines — and nothing else\n    }\n}",
  choices: [
    "Single Responsibility — it has one clearly bounded job and one reason to change",
    "Dependency Inversion",
    "Polymorphism",
    "Open/Closed",
  ],
  answer: 0,
  explain: "Single Responsibility: the class does exactly one thing — turn a loan list into a deduplicated, sorted list of safety briefings — and would only change if that briefing logic changes. It does not print, persist, or apply lending rules. A class with one reason to change is easy to test and reuse; cite its single Generate method as the evidence.",
},
{
  id: "th-16", cat: "Exam Theory", type: "mc",
  q: "Question 1.2 asks the PURPOSE of the interfaces (IEventLog, IStaffNotifier, IToolCatalog). What is the best answer?",
  choices: [
    "They decouple the high-level workflow from concrete implementations, so you can swap or stub them without touching callers",
    "They make the program run faster",
    "They are required by C# for every class",
    "They store the application's data",
  ],
  answer: 0,
  explain: "Interfaces here are contracts that let the high-level logic depend on WHAT a collaborator does, not HOW. Because IntakeDesk talks to IEventLog rather than ConsoleEventLog, you can swap a file logger or a silent test double with no change to the desk. That is abstraction plus Dependency Inversion, and it makes the system testable and extensible — exactly the point to make in 1.2.",
},
{
  id: "th-17", cat: "Exam Theory", type: "code-mc",
  q: "Which design pattern is this?",
  code: "public sealed class CenterClock\n{\n    private static CenterClock? _instance;\n    private CenterClock() { }\n    public static CenterClock Instance => _instance ??= new CenterClock();\n}",
  choices: [
    "Singleton — a private constructor plus a static accessor guarantee one shared instance",
    "Factory Method — it creates a family of products",
    "Strategy — interchangeable algorithms",
    "Adapter — it adapts one interface to another",
  ],
  answer: 0,
  explain: "Singleton: the constructor is private (no other code can `new` it) and a static Instance property lazily creates and returns the one and only instance. Use it when exactly one shared object is wanted — a clock, a config, a logger. Be ready to note its downsides too: it is global state and makes testing harder, which graders like to see in a pattern discussion.",
},
{
  id: "th-18", cat: "Exam Theory", type: "code-mc",
  q: "Which design pattern lets LegacyPrinter be used where an IReceiptPrinter is expected?",
  code: "class LegacyPrinter { public void Emit(string raw) => Console.Write(raw); }\n\nclass LegacyPrinterAdapter : IReceiptPrinter\n{\n    private readonly LegacyPrinter _p = new();\n    public void Print(string message) => _p.Emit(message + \"\\n\");\n}",
  choices: [
    "Adapter — it wraps an incompatible type and exposes the interface the client expects",
    "Facade — it simplifies a whole subsystem",
    "Singleton — it guarantees one instance",
    "Observer — publish/subscribe notifications",
  ],
  answer: 0,
  explain: "Adapter: LegacyPrinterAdapter implements the IReceiptPrinter the client expects (Print) and forwards to the wrapped LegacyPrinter's incompatible Emit method, translating the call. It lets you reuse a class whose interface does not match without changing either side. Facade simplifies many parts behind one face; Adapter specifically makes one mismatched interface fit another.",
},
{
  id: "th-19", cat: "Exam Theory", type: "mc",
  q: "In SOLID, which principle says 'high-level modules should depend on abstractions, not on concrete details'?",
  choices: [
    "Dependency Inversion (the D)",
    "Single Responsibility (the S)",
    "Liskov Substitution (the L)",
    "Open/Closed (the O)",
  ],
  answer: 0,
  explain: "Dependency Inversion (D) says both high- and low-level code should depend on abstractions, not on concretions, and details depend on abstractions rather than the reverse. In the exam code, IntakeDesk depending on IEventLog (not ConsoleEventLog) is DIP. For contrast: S is one reason to change, O is extend without modifying, L is subtypes substitutable for their base.",
},
{
  id: "th-20", cat: "Exam Theory", type: "code-mc",
  q: "For the 2.1 MVVM write-up, what is the ViewModel's role here?",
  code: "public partial class MainWindowViewModel : ObservableObject\n{\n    [ObservableProperty] private int _pressure;\n    [RelayCommand] private void Fill() => Pressure++;\n}\n// View:  <TextBlock Text=\"{Binding Pressure}\"/>  <Button Command=\"{Binding FillCommand}\"/>",
  choices: [
    "It holds bindable state (Pressure) and commands (FillCommand) the View binds to; it has no UI controls and no reference to the View",
    "It is the XAML window the user sees",
    "It is the database that stores pressures",
    "It is the code-behind event handler",
  ],
  answer: 0,
  explain: "In MVVM the ViewModel exposes bindable state and commands and holds the presentation logic, with no reference to controls or the View. The View (AXAML) binds to it (Text to Pressure, Command to FillCommand) and the Model is the plain domain data. Keeping the ViewModel UI-free is exactly what makes it unit-testable — which is what Problem 3 then tests on a ViewModel like this.",
},

/* ==================== MOCK EXAM 1 (20 MCQs, 2026 format) ==================== */

{
  id: "me1-01", cat: "Mock Exam 1", type: "mc",
  q: "A class keeps its fields private and exposes a validating property with a private setter. Which OO principle is this?",
  choices: ["Encapsulation", "Polymorphism", "Inheritance", "Abstraction"],
  answer: 0,
  explain: "Encapsulation is information hiding: private fields plus a validating property and a private setter keep a class's internals out of reach so its invariants stay intact. Abstraction is the related but distinct idea of exposing what a type does while hiding how, usually via an interface. On exam code, point at private fields and private or init setters as your encapsulation evidence.",
},
{
  id: "me1-02", cat: "Mock Exam 1", type: "code-mc",
  q: "What is printed?",
  code: "public class Animal { public virtual string Speak() => \"...\"; }\npublic class Dog : Animal { public override string Speak() => \"Woof\"; }\n\nAnimal a = new Dog();\nConsole.WriteLine(a.Speak());",
  choices: [
    "Woof, because override makes the runtime type decide",
    "..., because the variable type Animal decides",
    "Compile error, because Animal is not abstract",
    "Woof... , because both versions run",
  ],
  answer: 0,
  explain: "With override, method resolution is dynamic: the runtime type (Dog) wins regardless of the variable's declared type (Animal), so it prints Woof. That is runtime polymorphism, exactly what the Problem 1 MCQs love to probe. Contrast with new, which hides instead of overrides and would resolve from the variable's compile-time type and print the base version instead.",
},
{
  id: "me1-03", cat: "Mock Exam 1", type: "mc",
  q: "An app swaps sorting algorithms at runtime by holding an ISortStrategy field and calling strategy.Sort(). Which design pattern is this?",
  choices: ["Strategy", "Observer", "Adapter", "Singleton"],
  answer: 0,
  explain: "Strategy captures a family of interchangeable algorithms behind one interface so the caller can swap behaviour at runtime without changing its own code. Holding an ISortStrategy and delegating to strategy.Sort() is the textbook shape. Observer notifies subscribers of state changes; Adapter converts one interface into another; Singleton restricts a type to a single instance.",
},
{
  id: "me1-04", cat: "Mock Exam 1", type: "code-mc",
  q: "What does the source generator create from this, and what must the View bind to?",
  code: "public partial class VM : ObservableObject\n{\n    [ObservableProperty] private string _name;\n}",
  choices: [
    "A public Name property that raises PropertyChanged; the View binds Name",
    "A private name property; the View binds _name",
    "Nothing until you write the property by hand",
    "A NameCommand; the View binds NameCommand",
  ],
  answer: 0,
  explain: "[ObservableProperty] on the field _name generates a public PascalCase property Name whose setter raises PropertyChanged, which is what data binding listens for. The View binds the generated property (Name), never the backing field. The class must be partial so the generator can add the member, and inherit ObservableObject for the change-notification plumbing.",
},
{
  id: "me1-05", cat: "Mock Exam 1", type: "mc",
  q: "What is the practical difference between a public delegate field and an event in C#?",
  choices: [
    "An event can only be raised inside the declaring class; a plain delegate field can be invoked by any outside code",
    "They are identical; event is just a keyword with no effect",
    "An event supports multiple subscribers but a delegate cannot",
    "A delegate is type-safe but an event is not",
  ],
  answer: 0,
  explain: "The event keyword restricts access: outside code may only subscribe (+=) or unsubscribe (-=), while raising or invoking is allowed solely inside the declaring class. A bare public delegate field lets anyone invoke or overwrite it, breaking encapsulation. Both are multicast and type-safe; the real win of an event is that controlled invocation, which is why Observer relies on it.",
},
{
  id: "me1-06", cat: "Mock Exam 1", type: "code-mc",
  q: "Which SOLID principle does this class most clearly violate?",
  code: "class Report\n{\n    public string Render() { /* ... */ }\n    public void SaveToDisk(string path) { /* ... */ }\n    public void Email(string to) { /* ... */ }\n}",
  choices: [
    "Single Responsibility, because it renders, persists, and emails",
    "Liskov Substitution",
    "Interface Segregation",
    "Dependency Inversion",
  ],
  answer: 0,
  explain: "Single Responsibility says a class should have one reason to change. Report mixes three: formatting, file persistence, and email delivery, so a change to any one forces edits here and risks the others. Splitting it into a renderer, a repository, and a mailer gives each one reason to change. SRP is the most common Problem 1 finding, so practise naming the multiple jobs out loud.",
},
{
  id: "me1-07", cat: "Mock Exam 1", type: "mc",
  q: "You must use a third-party logger whose interface does not match your ILogger. You write a class implementing ILogger that forwards calls to the third-party type. Which pattern is this?",
  choices: ["Adapter", "Decorator", "Facade", "Strategy"],
  answer: 0,
  explain: "Adapter wraps an incompatible type and exposes an interface the client already expects, translating each call across. Implementing ILogger and forwarding to the third-party logger is exactly that. Decorator adds behaviour while keeping the same interface; Facade simplifies a whole subsystem behind one entry point; Strategy swaps interchangeable algorithms. The exam phrasing to recognise is wraps the service.",
},
{
  id: "me1-08", cat: "Mock Exam 1", type: "mc",
  q: "A prototype shows the full breadth of the UI (every screen and menu) but none of the buttons actually do anything. What kind of prototype is this?",
  choices: ["Horizontal prototype", "Vertical prototype", "High-fidelity prototype", "Throwaway prototype"],
  answer: 0,
  explain: "Resolution describes how much functionality a prototype implements. A horizontal prototype spreads wide across many features but shallow, so the whole interface is visible while little works underneath. A vertical prototype is the opposite: one complete flow working end to end. Fidelity is a separate axis about how finished it looks, so do not confuse horizontal with low-fidelity.",
},
{
  id: "me1-09", cat: "Mock Exam 1", type: "code-mc",
  q: "What does r contain?",
  code: "var r = books\n    .GroupBy(b => b.Author)\n    .Select(g => new { Author = g.Key, Count = g.Count() })\n    .OrderByDescending(x => x.Count)\n    .ToList();",
  choices: [
    "Each author with how many books they wrote, most-prolific first",
    "The single author with the most books",
    "Every book sorted by author name",
    "The total number of authors",
  ],
  answer: 0,
  explain: "GroupBy(b => b.Author) buckets books by author; Select projects each bucket to its key plus g.Count(); OrderByDescending sorts by that count. The result is one row per author with their book count, highest first. This is the exact shape of the practice exam's number-of-comics-per-author query, so recognise GroupBy then Count then OrderByDescending on sight.",
},
{
  id: "me1-10", cat: "Mock Exam 1", type: "code-mc",
  q: "What is printed?",
  code: "var q = new Queue<int>();\nq.Enqueue(1); q.Enqueue(2); q.Enqueue(3);\nConsole.Write(q.Dequeue());\nConsole.Write(q.Dequeue());",
  choices: ["12", "32", "21", "23"],
  answer: 0,
  explain: "A Queue is FIFO: first in, first out. Enqueue adds to the back and Dequeue removes from the front, so items leave in the order they arrived, 1 then 2, printing 12. Contrast with a Stack, which is LIFO (Push and Pop at the same end) and would serve the last items first. Knowing which end each structure touches is a recurring trace question.",
},
{
  id: "me1-11", cat: "Mock Exam 1", type: "mc",
  q: "A background timer computes a new value. Why must the ViewModel update a bound property on the UI thread (e.g. via Dispatcher.UIThread.Post)?",
  choices: [
    "UI state may only be touched on the UI thread; updating from another thread can crash or corrupt the UI",
    "Dispatcher makes the code run faster",
    "It is only needed for animations",
    "Background threads cannot do arithmetic",
  ],
  answer: 0,
  explain: "Avalonia, like other UI frameworks, is single-threaded for the UI: controls and the bindings that feed them may only be touched on the UI thread. A background worker that writes a bound property directly can throw or leave the UI inconsistent. Dispatcher.UIThread.Post marshals the update back onto the UI thread. This is the thread-safety point Problem 3 explicitly awards marks for.",
},
{
  id: "me1-12", cat: "Mock Exam 1", type: "code-mc",
  q: "What does this test assert?",
  code: "Assert.Throws<InvalidOperationException>(() => account.Withdraw(999));",
  choices: [
    "That calling Withdraw(999) throws an InvalidOperationException",
    "That Withdraw returns 999",
    "That no exception is thrown",
    "That the test is skipped",
  ],
  answer: 0,
  explain: "Assert.Throws<T> passes only if the delegate it runs throws exactly that exception type (or a subclass), and it returns the caught exception so you can inspect its message. Here it verifies that an over-limit withdrawal is rejected with InvalidOperationException. Testing the failure path like this matters as much as the happy path and shows up in the unit-testing problem.",
},
{
  id: "me1-13", cat: "Mock Exam 1", type: "mc",
  q: "In this course, which statement about interfaces versus abstract classes is correct?",
  choices: [
    "A class can implement many interfaces but inherit only one base class",
    "A class can inherit many base classes but implement one interface",
    "Interfaces can hold fields and constructors",
    "Abstract classes cannot have any implemented methods",
  ],
  answer: 0,
  explain: "C# allows single class inheritance but unlimited interface implementation, so Duck : Animal, IFlyable, ISwimmable is legal. Interfaces here are pure can-do contracts with no fields or constructors, which is why many unrelated types can share one. An abstract class can mix abstract members with fully implemented ones and shared state, but you only get to inherit one.",
},
{
  id: "me1-14", cat: "Mock Exam 1", type: "code-mc",
  q: "When does the filtering actually run?",
  code: "var q = list.Where(x => x > 0);",
  choices: [
    "When q is enumerated (e.g. ToList or foreach), not at the line shown",
    "Immediately on the Where line",
    "Never, unless you call .Run()",
    "Once per item as you build the query",
  ],
  answer: 0,
  explain: "LINQ operators like Where are lazy: they build a pipeline but defer execution until you enumerate the result, via foreach, ToList, First, Count, and so on. Until then nothing is filtered, and the query re-runs against the current source each time you enumerate it. Calling ToList materialises a snapshot, which is why exam solutions often end a query with ToList.",
},
{
  id: "me1-15", cat: "Mock Exam 1", type: "code-mc",
  q: "What does the View bind its Button.Command to?",
  code: "[RelayCommand]\nprivate void AddItem() { /* ... */ }",
  choices: ["AddItemCommand", "AddItem", "AddItemRelayCommand", "Command_AddItem"],
  answer: 0,
  explain: "[RelayCommand] on a method named AddItem generates a public IRelayCommand property called AddItemCommand: the method name plus a Command suffix. The Button binds Command to AddItemCommand, not to the method. If a command binding silently does nothing, the usual cause is binding the bare method name or forgetting the Command suffix the generator adds.",
},
{
  id: "me1-16", cat: "Mock Exam 1", type: "code-mc",
  q: "Why is the attribute here?",
  code: "public class Comic\n{\n    [JsonPropertyName(\"release_year\")]\n    public int ReleaseYear { get; set; }\n}",
  choices: [
    "It maps the JSON key release_year onto the C# property ReleaseYear",
    "It renames the property at runtime",
    "It makes the property read-only",
    "It is required for every property",
  ],
  answer: 0,
  explain: "System.Text.Json matches JSON keys to property names case-insensitively but cannot bridge genuinely different names. When the JSON key is release_year and the property is ReleaseYear, [JsonPropertyName(\"release_year\")] tells the serializer they are the same field. Without it the property stays at its default of 0. You only need it when the names actually differ, not on every property.",
},
{
  id: "me1-17", cat: "Mock Exam 1", type: "code-mc",
  q: "Which SOLID principle does taking IEmailSender in the constructor demonstrate?",
  code: "class OrderService\n{\n    private readonly IEmailSender _sender;\n    public OrderService(IEmailSender sender) => _sender = sender;\n}",
  choices: [
    "Dependency Inversion: depend on an abstraction, injected from outside",
    "Single Responsibility",
    "Liskov Substitution",
    "Open/Closed",
  ],
  answer: 0,
  explain: "Dependency Inversion says high-level code should depend on abstractions, not concrete classes. OrderService depending on IEmailSender (not SmtpEmailSender) and receiving it through the constructor is textbook DIP plus constructor injection. The payoff is testability: a test passes a fake IEmailSender. In exam ViewModels, an injected IRepository is the same pattern to name.",
},
{
  id: "me1-18", cat: "Mock Exam 1", type: "code-mc",
  q: "What is the value of d?",
  code: "object o = \"hello\";\nDog d = o as Dog;",
  choices: [
    "null, because as returns null when the cast fails instead of throwing",
    "A new empty Dog",
    "It throws InvalidCastException",
    "It does not compile",
  ],
  answer: 0,
  explain: "The as operator attempts a reference conversion and yields null on failure rather than throwing, which is why it is always paired with a null check. A hard cast (Dog)o would instead throw InvalidCastException. The modern preferred form is pattern matching: if (o is Dog dog) { ... } both tests and binds in one step. Here o is a string, so d is null.",
},
{
  id: "me1-19", cat: "Mock Exam 1", type: "code-mc",
  q: "What is printed?",
  code: "var ages = new Dictionary<string, int>();\nages[\"Ann\"] = 30;\nages[\"Ann\"] = 31;\nConsole.Write(ages.Count);",
  choices: ["1", "2", "0", "It throws an exception"],
  answer: 0,
  explain: "Dictionary keys are unique. Assigning ages[\"Ann\"] a second time updates the existing entry rather than adding a new one, so Count stays 1 and the stored value becomes 31. Calling Add(\"Ann\", ...) twice would instead throw because the key already exists; the indexer is the upsert-friendly form. Distinguishing indexer-assign from Add is a common trap.",
},
{
  id: "me1-20", cat: "Mock Exam 1", type: "code-mc",
  q: "What is the effect of await Task.Delay(100) inside this loop?",
  code: "async Task CountAsync()\n{\n    for (int i = 1; i <= 3; i++)\n    {\n        Count = i;\n        await Task.Delay(100);\n    }\n}",
  choices: [
    "It pauses 100ms between updates without blocking the UI thread",
    "It blocks the UI thread for 100ms each loop",
    "It runs the whole loop on a background thread",
    "It makes the loop run three times instantly",
  ],
  answer: 0,
  explain: "await Task.Delay(100) waits asynchronously and returns control to the caller meanwhile, so the UI stays responsive and the loop resumes after each delay on the captured context. It does not freeze the thread the way Thread.Sleep would, and the method continues on the UI context unless told otherwise. This is the async counter shape behind Problem 3's Start button.",
},

/* ==================== MOCK EXAM 2 (20 MCQs, 2026 format) ==================== */

{
  id: "me2-01", cat: "Mock Exam 2", type: "mc",
  q: "A University owns Departments and they vanish if it closes; a Library holds Books that can move to another library. Which describes the University-to-Department link?",
  choices: [
    "Composition, because the part cannot exist without the whole",
    "Aggregation, because the part can exist independently",
    "Inheritance",
    "Polymorphism",
  ],
  answer: 0,
  explain: "Composition is a strong has-a where the part's lifetime is bound to the whole: close the University and its Departments are gone. Aggregation is a weaker has-a where the part can outlive or move between wholes, like Books between libraries. Both are has-a, not is-a, so neither is inheritance. The lifetime test, does the part survive the whole, tells them apart.",
},
{
  id: "me2-02", cat: "Mock Exam 2", type: "code-mc",
  q: "What happens when you compile this?",
  code: "public abstract class Shape { public abstract double Area(); }\npublic class Circle : Shape { }",
  choices: [
    "Compile error, because Circle must override the abstract Area()",
    "It compiles and Area returns 0",
    "It compiles and Circle becomes abstract automatically",
    "It compiles but throws at runtime when Area is called",
  ],
  answer: 0,
  explain: "An abstract method has no body and forms a contract every concrete subclass must fulfil. Circle is declared concrete yet does not implement Area(), so the compiler rejects it. Either implement Area() in Circle with override, or mark Circle abstract too and push the obligation further down. This compile-time enforcement is the entire point of abstract members.",
},
{
  id: "me2-03", cat: "Mock Exam 2", type: "mc",
  q: "Which statement best describes the Observer pattern?",
  choices: [
    "The Subject keeps a list of observers and notifies them when its state changes",
    "Observers poll the Subject in a loop to detect changes",
    "The Subject and observers share a database they both read",
    "Each observer modifies the Subject directly",
  ],
  answer: 0,
  explain: "In Observer, the Subject maintains a list of subscribers and pushes a notification to each when its state changes, so observers react without polling. In C# this is typically built on events: subscribers += a handler and the Subject raises the event. It underpins MVVM change notification, where the View observes the ViewModel through PropertyChanged. Push, not poll, is the key idea.",
},
{
  id: "me2-04", cat: "Mock Exam 2", type: "mc",
  q: "What is the core difference between MVC and MVVM?",
  choices: [
    "MVC uses a Controller to mediate View and Model; MVVM uses a ViewModel with data binding",
    "MVVM removes all business logic from the Model",
    "MVVM is just a newer name for MVC with the same structure",
    "MVC keeps state in the Controller while MVVM keeps it in the Model",
  ],
  answer: 0,
  explain: "Both separate presentation from data, but the mediator differs. MVC routes user input through a Controller that updates the Model and picks a View. MVVM places a ViewModel between View and Model and connects them with two-way data binding, so the View binds directly to ViewModel properties and commands instead of calling controller actions. The binding layer is what distinguishes MVVM.",
},
{
  id: "me2-05", cat: "Mock Exam 2", type: "code-mc",
  q: "Where can Clicked be raised (invoked)?",
  code: "class Button\n{\n    public event EventHandler Clicked;\n}",
  choices: [
    "Only inside Button, the class that declares it",
    "Anywhere that can see the Button instance",
    "Only inside handlers subscribed to it",
    "Only on the UI thread",
  ],
  answer: 0,
  explain: "An event may be invoked only within the type that declares it; external code can solely subscribe with += or unsubscribe with -=. So Clicked can be raised inside Button, typically via an OnClicked helper, but not by callers holding a Button. That asymmetry is exactly what the delegate-versus-event distinction protects, and it keeps the Observer Subject in charge of when notifications fire.",
},
{
  id: "me2-06", cat: "Mock Exam 2", type: "code-mc",
  q: "Which principle does this most clearly violate, and what is the fix?",
  code: "double Area(Shape s)\n{\n    switch (s.Type)\n    {\n        case \"circle\": return Math.PI * s.R * s.R;\n        case \"square\": return s.Side * s.Side;\n    }\n    return 0;\n}",
  choices: [
    "Open/Closed: make Shape abstract with an Area() each subclass overrides",
    "Liskov: replace inheritance with composition",
    "Single Responsibility: split Area into two methods",
    "Interface Segregation: add more interfaces",
  ],
  answer: 0,
  explain: "Open/Closed says code should be open to extension but closed to modification. This switch must be edited every time a new shape appears, so each addition risks the existing cases. The polymorphic fix makes Shape declare an abstract Area() that Circle and Square each override; adding a Triangle is then a new class with no edits to existing code. Type-tag switches are the classic OCP smell.",
},
{
  id: "me2-07", cat: "Mock Exam 2", type: "mc",
  q: "A class needs to create objects but should not hard-code which concrete type, so it delegates creation to a method that returns the right type behind a shared interface. Which pattern is this?",
  choices: ["Factory", "Observer", "Adapter", "Decorator"],
  answer: 0,
  explain: "A Factory centralises object creation behind a method or type that returns instances through a common interface, so callers ask for what they need without naming concrete classes or using new directly. That decouples client code from specific implementations and keeps creation logic in one place. Observer is about notifications, Adapter about interface translation, Decorator about layering behaviour.",
},
{
  id: "me2-08", cat: "Mock Exam 2", type: "mc",
  q: "In prototyping, what does fidelity refer to (as opposed to resolution)?",
  choices: [
    "How finished and realistic the prototype looks",
    "How much functionality actually works",
    "How many users tested it",
    "How long it took to build",
  ],
  answer: 0,
  explain: "Prototypes vary on two independent axes. Fidelity is about appearance: how close the look and feel are to the finished product, from rough sketches (low) to pixel-perfect mockups (high). Resolution is the separate axis of how much actually functions, which is where horizontal versus vertical lives. A prototype can be high-fidelity yet low-resolution: it looks real but nothing works.",
},
{
  id: "me2-09", cat: "Mock Exam 2", type: "code-mc",
  q: "What does top hold?",
  code: "var top = players\n    .Where(p => p.Active)\n    .OrderByDescending(p => p.Score)\n    .First();",
  choices: [
    "The active player with the highest score",
    "The first active player in list order",
    "All active players sorted by score",
    "The highest score value",
  ],
  answer: 0,
  explain: "The pipeline filters to active players, sorts them by score from high to low, then First takes the top of that ordering, so top is the highest-scoring active player object, not the score itself. Note First throws if no player is active; FirstOrDefault would return null instead. Reading a Where then OrderByDescending then First chain as get the best matching record is a frequent MCQ.",
},
{
  id: "me2-10", cat: "Mock Exam 2", type: "code-mc",
  q: "What is printed?",
  code: "var s = new Stack<string>();\ns.Push(\"a\"); s.Push(\"b\"); s.Push(\"c\");\nConsole.Write(s.Pop());\nConsole.Write(s.Peek());",
  choices: ["cb", "ab", "ca", "cc"],
  answer: 0,
  explain: "A Stack is LIFO. Push adds to the top, so after pushing a, b, c the top is c. Pop removes and returns the top (c). Peek then reads the new top without removing it (b). So the output is cb. The distinction matters: Pop mutates the stack while Peek only looks, a detail trace questions exploit. A Queue, by contrast, would serve a first.",
},
{
  id: "me2-11", cat: "Mock Exam 2", type: "mc",
  q: "Why does calling a long, synchronous Thread.Sleep(5000) inside a button handler freeze the whole UI?",
  choices: [
    "It blocks the UI thread, so nothing renders or responds until it returns",
    "It uses too much memory",
    "It runs on a background thread that starves the UI",
    "Sleep is not allowed in Avalonia",
  ],
  answer: 0,
  explain: "The UI thread runs a single message loop that processes input, layout, and rendering. A synchronous Thread.Sleep on that thread blocks the loop entirely, so nothing redraws or responds until it returns, which the user sees as a frozen window. The fix is to await an asynchronous wait like Task.Delay, which yields the thread back to the loop while waiting. Never block the UI thread.",
},
{
  id: "me2-12", cat: "Mock Exam 2", type: "mc",
  q: "An exam test simulates clicking the increment button 100 times then checks the displayed number. What kind of test is this and what does it need?",
  choices: [
    "A headless Avalonia UI test that drives real controls and asserts the output",
    "A plain unit test that needs no UI at all",
    "A performance benchmark",
    "A manual test that cannot be automated",
  ],
  answer: 0,
  explain: "Driving actual buttons and reading a TextBlock requires the controls to exist, so this is a headless Avalonia UI test using Avalonia.Headless.XUnit, which spins up the app without a visible window. Plain ViewModel unit tests skip the UI and call commands directly; they are faster but cannot prove a button is wired. Problem 3 asks for both kinds, so know when each applies.",
},
{
  id: "me2-13", cat: "Mock Exam 2", type: "mc",
  q: "A base class has several subclasses; each overrides the same method and the correct version runs based on the actual object at runtime. Which principle enables this?",
  choices: ["Polymorphism", "Encapsulation", "Composition", "Abstraction"],
  answer: 0,
  explain: "Runtime polymorphism lets one call dispatch to the overriding implementation of whatever concrete type the object actually is, decided at runtime through virtual and override. It is what lets you treat a list of Shape as one type yet get each subclass's own Area(). Encapsulation hides state, abstraction hides how behind an interface, composition is has-a; none of those is the dispatch mechanism.",
},
{
  id: "me2-14", cat: "Mock Exam 2", type: "code-mc",
  q: "Why is DefaultIfEmpty(0) here?",
  code: "var avg = items\n    .Where(i => i.Price < 100)\n    .Select(i => i.Price)\n    .DefaultIfEmpty(0)\n    .Average();",
  choices: [
    "To stop Average throwing when no item costs under 100, by averaging {0}",
    "To replace every price with 0",
    "To sort the prices",
    "It has no effect",
  ],
  answer: 0,
  explain: "Average throws InvalidOperationException on an empty sequence. If no item is under 100 the filtered sequence is empty, so DefaultIfEmpty(0) injects a single 0 to average instead of crashing, yielding 0. It only kicks in when the sequence is otherwise empty; with data present it changes nothing. This guard is the trap the practice exam's category-average LINQ question hinges on.",
},
{
  id: "me2-15", cat: "Mock Exam 2", type: "mc",
  q: "Why bind a ListBox to an ObservableCollection<T> rather than a List<T>?",
  choices: [
    "ObservableCollection raises CollectionChanged so the UI updates as items are added or removed",
    "List is not generic",
    "ObservableCollection sorts itself",
    "List cannot store reference types",
  ],
  answer: 0,
  explain: "An ObservableCollection implements INotifyCollectionChanged, so adding or removing an item raises CollectionChanged and the bound ListBox updates itself with no extra code. A plain List sends no notifications, so the UI never reflects later changes. Note that replacing the whole collection instance can still break a binding, which is why exam ViewModels expose a get-only ObservableCollection and mutate it in place.",
},
{
  id: "me2-16", cat: "Mock Exam 2", type: "mc",
  q: "A JSON dataset has some objects missing the optional arrivalDate field. How should the C# model handle this safely?",
  choices: [
    "Make the property nullable (DateTime?) so a missing value deserializes to null",
    "Throw if any field is missing",
    "Use a non-nullable DateTime defaulted to today",
    "Skip those objects entirely",
  ],
  answer: 0,
  explain: "When a JSON key may be absent, the matching C# property should be nullable (DateTime?, string?, or a nullable list) so a missing value deserializes to null instead of a surprising default or an error. The query side then guards with ?. and ?? as needed. The spaceship exam explicitly warns of missing values, and currently-travelling ships are found exactly by arrivalDate == null.",
},
{
  id: "me2-17", cat: "Mock Exam 2", type: "code-mc",
  q: "Which principle is violated, and how is it fixed?",
  code: "interface IMachine { void Print(); void Scan(); void Fax(); }\nclass SimplePrinter : IMachine\n{\n    // Scan and Fax throw NotImplementedException\n}",
  choices: [
    "Interface Segregation: split IMachine into IPrinter, IScanner, IFax",
    "Liskov: SimplePrinter should not inherit at all",
    "Single Responsibility: merge the methods",
    "Dependency Inversion: inject IMachine",
  ],
  answer: 0,
  explain: "Interface Segregation says no client should be forced to depend on methods it does not use. A fat IMachine makes SimplePrinter implement Scan and Fax it cannot honour, so it throws, which also breaks Liskov downstream. Splitting into focused interfaces (IPrinter, IScanner, IFax) lets each type implement only what it truly supports. Throwing NotImplemented from an interface method is the classic ISP smell.",
},
{
  id: "me2-18", cat: "Mock Exam 2", type: "code-mc",
  q: "What is this assignment called and is it safe?",
  code: "Dog d = new Dog();\nAnimal a = d;",
  choices: [
    "Upcasting, which is implicit and always safe",
    "Downcasting, which needs an explicit cast and can fail",
    "Boxing, which wraps a value type",
    "Member hiding",
  ],
  answer: 0,
  explain: "Assigning a derived instance to a base-typed variable is an upcast: implicit and always safe, because a Dog has every member an Animal does. The reverse, treating an Animal variable as a Dog, is a downcast that needs an explicit cast and can throw if the object is not really a Dog. Boxing is the unrelated value-to-object conversion. Passing a Jet where Airplane is expected is the same safe upcast.",
},
{
  id: "me2-19", cat: "Mock Exam 2", type: "code-mc",
  q: "What is printed, and what about iteration order?",
  code: "var set = new HashSet<int>();\nset.Add(5); set.Add(5); set.Add(7);\nConsole.Write(set.Count);",
  choices: [
    "2, and iteration order is not guaranteed",
    "3, because duplicates are kept",
    "2, and items always iterate in insertion order",
    "It throws on the second Add",
  ],
  answer: 0,
  explain: "A HashSet stores only distinct values, so the second Add(5) is a no-op and Count is 2; Add returns false rather than throwing on a duplicate. Crucially its iteration order is not guaranteed and should not be relied on; if you need order, use a List or sort on the way out. Fast membership tests (O(1) Contains) are its strength, which is why it suits dedup and lookup tasks.",
},
{
  id: "me2-20", cat: "Mock Exam 2", type: "mc",
  q: "Problem 3 asks for a counter that increments every 100ms and can pause and resume. Why is a DispatcherTimer a natural fit?",
  choices: [
    "Its Tick fires on the UI thread at the interval, so updates are thread-safe and Start/Stop pause and resume it",
    "It runs on a background thread for speed",
    "It guarantees exactly 100ms with no drift",
    "It avoids needing any ViewModel state",
  ],
  answer: 0,
  explain: "A DispatcherTimer raises Tick on the UI thread at the set Interval, so incrementing a bound property in the handler is already on the right thread with no manual marshalling. Stop pauses it while keeping the current count, and Start resumes from there, which matches the Start, Pause, Reset spec exactly. An async Task loop also works, but then you must marshal updates back to the UI thread yourself.",
},

];

global.QUIZ_BANK = BANK;
if (typeof module !== "undefined" && module.exports) module.exports = BANK;

})(typeof window !== "undefined" ? window : globalThis);
